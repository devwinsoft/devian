// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using Newtonsoft.Json;
using Devian;

namespace Devian.Domain.Game
{
    // ========== Enums (gen:) ==========

    /// <summary>Auto-generated enum from TB_GAME_ERROR.id</summary>
    public enum GAME_ERROR_TYPE : int
    {
        SUCCESS = 0,
        ABILITY_ITEM_TABLE_NOT_FOUND = 1,
        ABILITY_UNIT_TABLE_NOT_FOUND = 2,
        ACCOUNT_SWITCH_GUEST_UPGRADE_FAILED = 3,
        ACCOUNT_SWITCH_LOGIN_FAILED = 4,
        ACCOUNT_SWITCH_UNSUPPORTED = 5,
        ACHIEVE_NOT_CLAIMABLE = 6,
        ACHIEVE_NOT_FOUND = 7,
        ACHIEVE_RUNTIME_MISSING = 8,
        ADS_COOLDOWN_ACTIVE = 9,
        ADS_LOAD_FAILED = 10,
        ADS_NOT_INITIALIZED = 11,
        ADS_PLACEMENT_NOT_FOUND = 12,
        ADS_PROVIDER_FAILED = 13,
        ADS_SHOW_FAILED = 14,
        ATTEND_CYCLE_COMPLETED = 15,
        ATTEND_DAY_MISMATCH = 16,
        ATTEND_NOT_CLAIMABLE = 17,
        ATTEND_NOT_FOUND = 18,
        ATTEND_NOT_INITIALIZED = 19,
        ATTEND_REWARD_APPLY_FAILED = 20,
        CLOUDSAVE_CHECKSUM = 21,
        CLOUDSAVE_CONNECTION_FAILED = 22,
        CLOUDSAVE_DELETE = 23,
        CLOUDSAVE_KEYIV = 24,
        CLOUDSAVE_LOAD = 25,
        CLOUDSAVE_NOCLIENT = 26,
        CLOUDSAVE_PAYLOAD_INVALID = 27,
        CLOUDSAVE_SAVE = 28,
        CLOUDSAVE_SIGNIN = 29,
        CLOUDSAVE_SLOT_EMPTY = 30,
        CLOUDSAVE_SLOT_MISSING = 31,
        FIREBASE_APPLE_BRIDGE = 32,
        FIREBASE_APPLE_SIGNIN = 33,
        FIREBASE_APPLE_TOKEN = 34,
        FIREBASE_DEPENDENCY = 35,
        FIREBASE_FACEBOOK_NOT_IMPLEMENTED = 36,
        FIREBASE_GOOGLE_REAUTH_REQUIRED = 37,
        FIREBASE_GOOGLE_SIGNIN = 38,
        FIREBASE_GOOGLE_TOKEN = 39,
        FIREBASE_NO_USER = 40,
        FIREBASE_NOT_INITIALIZED = 41,
        FIREBASE_REAUTH_UNKNOWN = 42,
        FIREBASE_SIGNIN = 43,
        FIREBASE_TOKEN = 44,
        GAME_INVALID_ARGUMENT = 45,
        GAME_SERVER_TIME_UNAVAILABLE = 46,
        IAP_NOT_INITIALIZED = 47,
        IAP_NOT_SUPPORTED = 48,
        INVENTORY_DELTA_AMOUNT_NEGATIVE = 49,
        INVENTORY_DELTA_ID_EMPTY = 50,
        INVENTORY_DELTA_TYPE_INVALID = 51,
        INVENTORY_DELTAS_NULL = 52,
        INVENTORY_REFUND_INSUFFICIENT = 53,
        LEADERBOARD_MESSAGE_NOT_FOUND = 54,
        LEADERBOARD_MESSAGE_SAVE_TYPE_INVALID = 55,
        LEADERBOARD_NOT_FOUND = 56,
        LEADERBOARD_PLATFORM_ID_MISSING = 57,
        LEADERBOARD_SEASON_NOT_ACTIVE = 58,
        LEADERBOARD_SEASON_NOT_FOUND = 59,
        LEADERBOARD_SEASON_TIME_INVALID = 60,
        LEADERBOARD_SERVER_TIME_UNAVAILABLE = 61,
        LOCALSAVE_CANCELLED = 62,
        LOCALSAVE_CHECKSUM = 63,
        LOCALSAVE_FILENAME_EMPTY = 64,
        LOCALSAVE_FILENAME_INVALID = 65,
        LOCALSAVE_KEYIV = 66,
        LOCALSAVE_NOT_FOUND = 67,
        LOCALSAVE_PATH_EMPTY = 68,
        LOCALSAVE_READ = 69,
        LOCALSAVE_SLOT_EMPTY = 70,
        LOCALSAVE_SLOT_MISSING = 71,
        LOCALSAVE_WRITE = 72,
        LOGIN_ANONYMOUS_EXCEPTION = 73,
        LOGIN_ANONYMOUS_FAILED = 74,
        LOGIN_APPLE_EXCEPTION = 75,
        LOGIN_APPLE_LINK_FAILED = 76,
        LOGIN_APPLE_MISSING_TOKEN = 77,
        LOGIN_APPLE_SIGNIN_FAILED = 78,
        LOGIN_CREDENTIAL_UNSUPPORTED = 79,
        LOGIN_FIREBASE_DEPENDENCY = 80,
        LOGIN_GOOGLE_EXCEPTION = 81,
        LOGIN_GOOGLE_LINK_FAILED = 82,
        LOGIN_GOOGLE_MISSING_AUTH_CODE = 83,
        LOGIN_GOOGLE_SIGNIN_FAILED = 84,
        LOGIN_GPGS_AUTH_FAILED = 85,
        LOGIN_GPGS_EXCEPTION = 86,
        LOGIN_GPGS_NO_AUTH_CODE = 87,
        LOGIN_GPGS_NO_AUTHENTICATE = 88,
        LOGIN_GPGS_NO_SERVER_ACCESS = 89,
        LOGIN_GPGS_NOT_FOUND = 90,
        LOGIN_GPGS_NOT_INITIALIZED = 91,
        LOGIN_SYNC_CANCELLED = 92,
        LOGIN_SYNC_LOAD_CLOUD_FAILED = 93,
        LOGIN_SYNC_LOAD_LOCAL_FAILED = 94,
        LOGIN_SYNC_RESOLVE_FAILED = 95,
        LOGIN_SYNC_SAVE_CLOUD_FAILED = 96,
        LOGIN_SYNC_SAVE_LOCAL_FAILED = 97,
        LOGIN_SYNC_SLOT_LIST_FAILED = 98,
        LOGIN_UNSUPPORTED = 99,
        MISSION_ALREADY_CLAIMED = 100,
        MISSION_NOT_CLAIMABLE = 101,
        MISSION_NOT_FOUND = 102,
        MISSION_RUNTIME_MISSING = 103,
        MISSION_RUNTIME_STALE = 104,
        PURCHASE_ADJUSTMENTS_CALL_FAILED = 105,
        PURCHASE_ADJUSTMENTS_INVALID_ARGUMENT = 106,
        PURCHASE_CLIENT_GRANT_PURCHASE_ID_EMPTY = 107,
        PURCHASE_ENTITLEMENTS_CALL_FAILED = 108,
        PURCHASE_FETCH_FAILED = 109,
        PURCHASE_FUNCTION_CALL_FAILED = 110,
        PURCHASE_FUNCTION_RESPONSE_INVALID = 111,
        PURCHASE_INIT_FAILED = 112,
        PURCHASE_INIT_REQUIRED = 113,
        PURCHASE_INTERNAL_PRODUCT_ID_EMPTY = 114,
        PURCHASE_INTERRUPTED_SNAPSHOT_KIND_INVALID = 115,
        PURCHASE_INTERRUPTED_SNAPSHOT_PRODUCT_ID_MISSING = 116,
        PURCHASE_INTERRUPTED_STORAGE_UNAVAILABLE = 117,
        PURCHASE_NETWORK_UNAVAILABLE = 118,
        PURCHASE_NOT_FOUND = 119,
        PURCHASE_PURCHASE_IN_PROGRESS = 120,
        PURCHASE_PURCHASE_REQUEST_FAILED = 121,
        PURCHASE_RECENT_CALL_FAILED = 122,
        PURCHASE_RECENT_NOT_FOUND = 123,
        PURCHASE_REFUND_APPLY_FAILED = 124,
        PURCHASE_RENTAL_LATEST_CALL_FAILED = 125,
        PURCHASE_RENTAL_LATEST_NOT_FOUND = 126,
        PURCHASE_RESTORE_FAILED = 127,
        PURCHASE_SEASON_END_SOON_BLOCKED = 128,
        PURCHASE_SEASON_PASS_ALREADY_OWNED = 129,
        PURCHASE_STORE_CONFIRM_ACK_CALL_FAILED = 130,
        PURCHASE_STORE_FAILED = 131,
        PURCHASE_STORE_NOT_SET = 132,
        PURCHASE_UNAUTHENTICATED = 133,
        PURCHASE_UNSUPPORTED_PLATFORM = 134,
        PURCHASE_VERIFY_CALL_FAILED = 135,
        PURCHASE_VERIFY_FAILED_PRECONDITION = 136,
        PURCHASE_VERIFY_INVALID_ARGUMENT = 137,
        PURCHASE_VERIFY_REJECTED_UNKNOWN = 138,
        PUSH_DUPLICATE_NOTIFICATION = 139,
        PUSH_LOCAL_NOT_FOUND = 140,
        PUSH_NOT_INITIALIZED = 141,
        PUSH_NOTIFICATION_CANCEL_FAILED = 142,
        PUSH_NOTIFICATION_SCHEDULE_FAILED = 143,
        PUSH_PERMISSION_DENIED = 144,
        PUSH_TABLE_NOT_LOADED = 145,
        PUSH_TEST_BLOCKED = 146,
        PUSH_TOKEN_FAILED = 147,
        PUSH_TOPIC_SUBSCRIBE_FAILED = 148,
        PUSH_TOPIC_UNSUBSCRIBE_FAILED = 149,
        PUSH_UNSUPPORTED_PLATFORM = 150,
        RECOVERY_DECODE_FAILED = 151,
        RECOVERY_HMAC_FAILED = 152,
        RECOVERY_UNSUPPORTED_VERSION = 153,
        SAVEDATA_PAYLOAD_PARSE_FAILED = 154,
        SAVEDATA_SYNC_CANCELLED = 155,
        SAVEDATA_SYNC_LOAD_LOCAL_FAILED = 156,
        SAVEDATA_SYNC_REQUIRED = 157,
        SAVEDATA_SYNC_RESOLVE_FAILED = 158,
        SAVEDATA_SYNC_SAVE_CLOUD_FAILED = 159,
        SAVEDATA_SYNC_SAVE_LOCAL_FAILED = 160,
        SHOP_ADS_NOT_AVAILABLE = 161,
        SHOP_ADS_SHOW_FAILED = 162,
        SHOP_BUY_FAILED = 163,
        SHOP_CAN_BUY_FAILED = 164,
        SHOP_CURRENCY_INSUFFICIENT = 165,
        SHOP_ITEM_DAILY_MANUAL_REFRESH_COUNT_EXHAUSTED = 166,
        SHOP_ITEM_PURCHASE_LIMIT_DISABLED = 167,
        SHOP_ITEM_PURCHASE_LIMIT_EXCEEDED = 168,
        SHOP_PRODUCT_ID_EMPTY = 169,
        SHOP_PRODUCT_NOT_FOUND = 170,
        SHOP_PRODUCT_PRICE_INVALID = 171,
        SHOP_REWARD_APPLY_FAILED = 172,
        SHOP_REWARD_GROUP_EMPTY = 173,
        SHOP_SERVER_TIME_UNAVAILABLE = 174,
        SHOP_WALLET_UNAVAILABLE = 175,
        TREASURE_CHEST_NOT_FOUND = 176,
        TREASURE_GRADE_INVALID = 177,
        TREASURE_REWARD_APPLY_FAILED = 178,
        TREASURE_REWARD_EMPTY = 179,
        TREASURE_REWARD_GROUP_EMPTY = 180,
        VERSION_CHECK_NETWORK_FAILED = 181,
        VERSION_CHECK_PARSE_FAILED = 182,
        VERSION_CHECK_RESPONSE_EMPTY = 183,
        VERSION_CHECK_URL_NOT_CONFIGURED = 184,
    }

    /// <summary>Auto-generated enum from TB_EQUIP_SLOT.equip_type</summary>
    public enum EQUIP_TYPE : int
    {
        BOW = 0,
        GLOVES = 1,
        HELMET = 2,
        RING = 3,
        SHIELD = 4,
        SWORD = 5,
    }

    // ================================================================
    // Contracts
    // ================================================================

    /// <summary>ACHIEVE_TYPE enum</summary>
    public enum ACHIEVE_TYPE
    {
        NONE = 0,
        SOCIAL = 1,
        PASS = 2,
    }

    /// <summary>ACHIEVE_MESSAGE_TYPE enum</summary>
    public enum ACHIEVE_MESSAGE_TYPE
    {
        NONE = 0,
        RUNTIME_INIT = 1,
        RUNTIME_PROGRESS = 2,
        RUNTIME_CLAIMABLE = 3,
        RUNTIME_LEVEL_UP = 4,
        RUNTIME_REWARDED = 5,
        RUNTIME_UNLOCKED = 6,
        RUNTIME_ACTIVE = 7,
    }

    /// <summary>GAME_MESSAGE_TYPE enum</summary>
    public enum GAME_MESSAGE_TYPE
    {
        NONE = 0,
        LOGIN = 1001,
        STAGE_CLEAR = 1002,
        STAGE_SCORE = 1003,
        MISSION_CLEAR = 1004,
        BATTLE_PASS_001 = 2001,
        BATTLE_PASS_002 = 2002,
        BATTLE_PASS_003 = 2003,
        BATTLE_PASS_004 = 2004,
        BATTLE_PASS_005 = 2005,
        TEST_001 = 8001,
        TEST_002 = 8002,
        TEST_003 = 8003,
        TEST_004 = 8004,
        TEST_005 = 8005,
        TEST_006 = 8006,
        ACHIEVE_UNLOCKED = 9000,
        ACHIEVE_001 = 9001,
        ACHIEVE_002 = 9002,
        ACHIEVE_003 = 9003,
        ACHIEVE_004 = 9004,
        ACHIEVE_005 = 9005,
    }

    /// <summary>GAME_MESSAGE_SAVE_TYPE enum</summary>
    public enum GAME_MESSAGE_SAVE_TYPE
    {
        NONE = 0,
        SESSION_SUM = 1,
        SESSION_MAX = 2,
        SESSION_MIN = 3,
        TOTAL_SUM = 4,
        TOTAL_MAX = 5,
        TOTAL_MIN = 6,
    }

    /// <summary>GAME_MESSAGE_OP_TYPE enum</summary>
    public enum GAME_MESSAGE_OP_TYPE
    {
        NONE = 0,
        EQ = 1,
        GTE = 2,
        LTE = 3,
    }

    /// <summary>EQUIP_SLOT_TYPE enum</summary>
    public enum EQUIP_SLOT_TYPE
    {
        NONE = 0,
        HAND_MAIN = 4001,
        HAND_SUB = 4002,
        HEAD = 4003,
        GLOVES = 4004,
        RING_LEFT = 4005,
        RING_RIGHT = 4006,
    }

    /// <summary>INVENTORY_LIST_CHANGE_TYPE enum</summary>
    public enum INVENTORY_LIST_CHANGE_TYPE
    {
        NONE = 0,
        ADD = 1,
        REMOVE = 2,
    }

    /// <summary>INVENTORY_MESSAGE_TYPE enum</summary>
    public enum INVENTORY_MESSAGE_TYPE
    {
        NONE = 0,
        INVENTORY_SNAPSHOT_CHANGED = 1,
        CURRENCY_CHANGED = 2,
        ITEM_CARD_CHANGED = 3,
        ITEM_CARD_LIST_CHANGED = 4,
        ITEM_EQUIP_CHANGED = 5,
        ITEM_EQUIP_LIST_CHANGED = 6,
        ITEM_MATERIAL_CHANGED = 7,
        ITEM_HERO_CHANGED = 8,
        ITEM_HERO_LIST_CHANGED = 9,
        ITEM_MATERIAL_LIST_CHANGED = 10,
        PASS_OWNERSHIP_CHANGED = 11,
        RENTAL_CHANGED = 12,
        TREASURE_STATE_CHANGED = 13,
    }

    /// <summary>INVENTORY_SNAPSHOT_CHANGE_REASON enum</summary>
    public enum INVENTORY_SNAPSHOT_CHANGE_REASON
    {
        NONE = 0,
        LOADED = 1,
        CLEARED = 2,
        IMPORTED = 3,
        MIGRATED = 4,
    }

    /// <summary>ITEM_GRADE_TYPE enum</summary>
    public enum ITEM_GRADE_TYPE
    {
        NONE = 0,
        COMMON = 1,
        RARE = 2,
        EPIC = 3,
        LEGENDARY = 4,
        MYTHIC = 5,
    }

    /// <summary>ADVERTISE_FORMAT enum</summary>
    public enum ADVERTISE_FORMAT
    {
        BANNER = 0,
        INTERSTITIAL = 1,
        REWARDED = 2,
        APP_OPEN = 3,
    }

    /// <summary>ADVERTISE_PROVIDER enum</summary>
    public enum ADVERTISE_PROVIDER
    {
        GOOGLE_MOBILE_ADS = 0,
        MOCK = 1,
    }

    /// <summary>CURRENCY_TYPE enum</summary>
    public enum CURRENCY_TYPE
    {
        ADS = 0,
        ARENA_COIN = 1,
        FREE = 2,
        FRIENDSHIP = 3,
        GOLD = 4,
        GUILD_COIN = 5,
        JEWEL = 6,
        JEWEL_FREE = 7,
        JEWEL_PAID = 8,
        STAMINA = 9,
    }

    /// <summary>LEADERBOARD_MODE enum</summary>
    public enum LEADERBOARD_MODE
    {
        NONE = 0,
        NORMAL = 1,
        HARDCORE = 2,
    }

    /// <summary>PURCHASE_KIND enum</summary>
    public enum PURCHASE_KIND
    {
        CONSUMABLE = 0,
        SUBSCRIPTION = 1,
        PASS = 2,
        RENTAL = 3,
    }

    /// <summary>REWARD_TYPE enum</summary>
    public enum REWARD_TYPE
    {
        CARD = 0,
        CURRENCY = 1,
        EQUIP = 2,
        HERO = 3,
        MATERIAL = 7,
        RENTAL = 4,
        PASS = 5,
        TREASURE = 6,
    }

    /// <summary>MISSION_TYPE enum</summary>
    public enum MISSION_TYPE
    {
        NONE = 0,
        DAILY = 1,
        WEEKLY = 2,
    }

    /// <summary>MISSION_MESSAGE_TYPE enum</summary>
    public enum MISSION_MESSAGE_TYPE
    {
        NONE = 0,
        RUNTIME_INIT = 1,
        RUNTIME_PROGRESS = 2,
        RUNTIME_CLAIMABLE = 3,
        RUNTIME_REWARDED = 4,
        DAY_RESET = 5,
        ACHIEVE_LEVEL_UP = 6,
    }

    /// <summary>SHOP_CATALOG_TYPE enum</summary>
    public enum SHOP_CATALOG_TYPE
    {
        NONE = 0,
        DAILY = 1,
        CHEST = 2,
        PURCHASE = 3,
        GOLD = 4,
        EVENT = 5,
    }

    /// <summary>SHOP_DISCOUNT_TYPE enum</summary>
    public enum SHOP_DISCOUNT_TYPE
    {
        NONE = 0,
        PER10 = 1,
        PER20 = 2,
        PER30 = 3,
        PER50 = 4,
    }

    /// <summary>SHOP_PRODUCT_TYPE enum</summary>
    public enum SHOP_PRODUCT_TYPE
    {
        NONE = 0,
        FREE = 1,
        ADS = 2,
        CURRENCY = 3,
        PURCHASE = 4,
    }

    /// <summary>SHOP_PRODUCT_CHEST_TYPE enum</summary>
    public enum SHOP_PRODUCT_CHEST_TYPE
    {
        NONE = 0,
        ADS = 1,
        ONE = 2,
        TEN = 3,
    }

    /// <summary>UNIT_HERO_TYPE enum</summary>
    public enum UNIT_HERO_TYPE
    {
        NONE = 0,
        HERO_001 = 1,
        HERO_002 = 2,
        HERO_003 = 3,
        HERO_004 = 4,
        HERO_005 = 5,
    }

    /// <summary>UNIT_STAT_TYPE enum</summary>
    public enum UNIT_STAT_TYPE
    {
        NONE = 0,
        AFFECT_ATK_PHY_ADD = 1001,
        AFFECT_ATK_PHY_PER = 1002,
        AFFECT_ATK_MAG_ADD = 1003,
        AFFECT_ATK_MAG_PER = 1004,
        AFFECT_DEF_PHY_ADD = 1005,
        AFFECT_DEF_PHY_PER = 1006,
        AFFECT_DEF_MAG_ADD = 1007,
        AFFECT_DEF_MAG_PER = 1008,
        AFFECT_HP_ADD = 1009,
        AFFECT_HP_PER = 1010,
        ITEM_ATK_PHY = 2001,
        ITEM_ATK_MAG = 2002,
        ITEM_DEF_PHY = 2003,
        ITEM_DEF_MAG = 2004,
        ITEM_AMOUNT = 2005,
        ITEM_HP = 2006,
        ITEM_LEVEL = 2007,
        UNIT_ATK_PHY = 3001,
        UNIT_ATK_MAG = 3002,
        UNIT_DEF_PHY = 3003,
        UNIT_DEF_MAG = 3004,
        UNIT_HP = 3005,
        UNIT_LEVEL = 3006,
    }

    /// <summary>UNIT_TYPE enum</summary>
    public enum UNIT_TYPE
    {
        NONE = 0,
        HERO = 1,
        MONSTER = 2,
    }

    /// <summary>UserProfile class</summary>
    public sealed class UserProfile : IEntity
    {
        public int id { get; set; }
        public string name { get; set; } = string.Empty;
    }

    // ================================================================
    // Table Entities
    // ================================================================

    /// <summary>ACHIEVE_SOCIAL row</summary>
    public sealed class ACHIEVE_SOCIAL : IEntityKey<int>
    {
        public int index { get; set; }
        public string achieve_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public int level { get; set; }
        public int order_num { get; set; }
        public string req_msg_id { get; set; } = string.Empty;
        public CBigInt? req_value { get; set; }
        public string condition_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE condition_op { get; set; }
        public CBigInt? condition_value { get; set; }
        public string reward_group_id { get; set; } = string.Empty;
        public string apple_achievement_id { get; set; } = string.Empty;
        public string google_achievement_id { get; set; } = string.Empty;

        public int GetKey() => index;
    }

    /// <summary>ACHIEVE_PASS row</summary>
    public sealed class ACHIEVE_PASS : IEntityKey<int>
    {
        public int index { get; set; }
        public string achieve_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public int level { get; set; }
        public int order_num { get; set; }
        public string req_season_id { get; set; } = string.Empty;
        public string req_pass_id { get; set; } = string.Empty;
        public string condition_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE condition_op { get; set; }
        public CBigInt? condition_value { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public int GetKey() => index;
    }

    /// <summary>REWARD row</summary>
    public sealed class REWARD : IEntityKey<int>
    {
        public int reward_num { get; set; }
        public string reward_group_id { get; set; } = string.Empty;
        public REWARD_TYPE type { get; set; }
        public string id { get; set; } = string.Empty;
        public int amount { get; set; }
        public float rate { get; set; }

        public int GetKey() => reward_num;
    }

    /// <summary>GAME_ERROR row</summary>
    public sealed class GAME_ERROR : IEntityKey<GAME_ERROR_TYPE>
    {
        public GAME_ERROR_TYPE id { get; set; }
        public string msg_key { get; set; } = string.Empty;
        public string msg { get; set; } = string.Empty;

        public GAME_ERROR_TYPE GetKey() => id;
    }

    /// <summary>GAME_MESSAGE row</summary>
    public sealed class GAME_MESSAGE : IEntityKey<string>
    {
        public string message_id { get; set; } = string.Empty;
        public GAME_MESSAGE_TYPE message_type { get; set; }
        public GAME_MESSAGE_SAVE_TYPE save_type { get; set; }

        public string GetKey() => message_id;
    }

    /// <summary>ITEM_HERO row</summary>
    public sealed class ITEM_HERO : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string unit_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_HERO_LEVEL row</summary>
    public sealed class ITEM_HERO_LEVEL : IEntityKey<int>
    {
        public int index { get; set; }
        public string item_id { get; set; } = string.Empty;
        public int item_level { get; set; }
        public UNIT_STAT_TYPE stat_type00 { get; set; }
        public int stat_value00 { get; set; }
        public UNIT_STAT_TYPE stat_type01 { get; set; }
        public int stat_value01 { get; set; }
        public UNIT_STAT_TYPE stat_type02 { get; set; }
        public int stat_value02 { get; set; }
        public UNIT_STAT_TYPE stat_type03 { get; set; }
        public int stat_value03 { get; set; }

        public int GetKey() => index;
    }

    /// <summary>EQUIP_SLOT row</summary>
    public sealed class EQUIP_SLOT : IEntityKey<EQUIP_TYPE>
    {
        public EQUIP_TYPE equip_type { get; set; }
        public List<EQUIP_SLOT_TYPE> allowed_slots { get; set; } = new();
        public bool two_handed { get; set; }

        public EQUIP_TYPE GetKey() => equip_type;
    }

    /// <summary>ITEM_EQUIP row</summary>
    public sealed class ITEM_EQUIP : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;
        public EQUIP_TYPE equip_type { get; set; }
        public string upgrade_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_EQUIP_LEVEL row</summary>
    public sealed class ITEM_EQUIP_LEVEL : IEntityKey<int>
    {
        public int index { get; set; }
        public string item_id { get; set; } = string.Empty;
        public int item_level { get; set; }
        public UNIT_STAT_TYPE stat_type00 { get; set; }
        public int stat_value00 { get; set; }
        public UNIT_STAT_TYPE stat_type01 { get; set; }
        public int stat_value01 { get; set; }
        public UNIT_STAT_TYPE stat_type02 { get; set; }
        public int stat_value02 { get; set; }
        public UNIT_STAT_TYPE stat_type03 { get; set; }
        public int stat_value03 { get; set; }

        public int GetKey() => index;
    }

    /// <summary>ITEM_CARD row</summary>
    public sealed class ITEM_CARD : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_CARD_LEVEL row</summary>
    public sealed class ITEM_CARD_LEVEL : IEntityKey<int>
    {
        public int index { get; set; }
        public string item_id { get; set; } = string.Empty;
        public int item_level { get; set; }
        public UNIT_STAT_TYPE stat_type00 { get; set; }
        public int stat_value00 { get; set; }
        public UNIT_STAT_TYPE stat_type01 { get; set; }
        public int stat_value01 { get; set; }
        public UNIT_STAT_TYPE stat_type02 { get; set; }
        public int stat_value02 { get; set; }
        public UNIT_STAT_TYPE stat_type03 { get; set; }
        public int stat_value03 { get; set; }

        public int GetKey() => index;
    }

    /// <summary>ITEM_MATERIAL row</summary>
    public sealed class ITEM_MATERIAL : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_RENTAL row</summary>
    public sealed class ITEM_RENTAL : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_PASS row</summary>
    public sealed class ITEM_PASS : IEntityKey<string>
    {
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => item_id;
    }

    /// <summary>ITEM_TREASURE row</summary>
    public sealed class ITEM_TREASURE : IEntityKey<string>
    {
        public string chest_num { get; set; } = string.Empty;
        public string item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string reward_group_id { get; set; } = string.Empty;

        public string GetKey() => chest_num;
    }

    /// <summary>ADVERTISE row</summary>
    public sealed class ADVERTISE : IEntityKey<string>
    {
        public string advertise_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public ADVERTISE_FORMAT format { get; set; }
        public ADVERTISE_PROVIDER provider { get; set; }
        public bool auto_load { get; set; }
        public int cooldown_sec { get; set; }
        public string android_ad_unit_id { get; set; } = string.Empty;
        public string ios_ad_unit_id { get; set; } = string.Empty;

        public string GetKey() => advertise_id;
    }

    /// <summary>ATTEND row</summary>
    public sealed class ATTEND : IEntityKey<string>
    {
        public string attend_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public int day { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public string GetKey() => attend_id;
    }

    /// <summary>LEADERBOARD row</summary>
    public sealed class LEADERBOARD : IEntityKey<string>
    {
        public string leaderboard_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public string message_id { get; set; } = string.Empty;
        public LEADERBOARD_MODE mode { get; set; }
        public string season_id { get; set; } = string.Empty;
        public string apple_leaderboard_id { get; set; } = string.Empty;
        public string google_leaderboard_id { get; set; } = string.Empty;

        public string GetKey() => leaderboard_id;
    }

    /// <summary>LEADERBOARD_REWARD row</summary>
    public sealed class LEADERBOARD_REWARD : IEntityKey<int>
    {
        public int index { get; set; }
        public string leaderboard_id { get; set; } = string.Empty;
        public long rank_from { get; set; }
        public long rank_to { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public int GetKey() => index;
    }

    /// <summary>SEASON row</summary>
    public sealed class SEASON : IEntityKey<string>
    {
        public string season_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;
        public CDateTime? start_utc_time { get; set; }
        public CDateTime? end_utc_time { get; set; }

        public string GetKey() => season_id;
    }

    /// <summary>MISSION_DAILY row</summary>
    public sealed class MISSION_DAILY : IEntityKey<string>
    {
        public string mission_id { get; set; } = string.Empty;
        public bool is_active { get; set; }
        public bool is_fixed { get; set; }
        public int order_num { get; set; }
        public string condition_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE condition_op { get; set; }
        public CBigInt? condition_value { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public string GetKey() => mission_id;
    }

    /// <summary>MISSION_WEEKLY row</summary>
    public sealed class MISSION_WEEKLY : IEntityKey<string>
    {
        public string mission_id { get; set; } = string.Empty;
        public int day { get; set; }
        public bool is_active { get; set; }
        public string condition_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE condition_op { get; set; }
        public CBigInt? condition_value { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public string GetKey() => mission_id;
    }

    /// <summary>SHOP_CATALOG row</summary>
    public sealed class SHOP_CATALOG : IEntityKey<SHOP_CATALOG_TYPE>
    {
        public SHOP_CATALOG_TYPE catalog_type { get; set; }
        public string name_id { get; set; } = string.Empty;
        public int auto_refresh_days { get; set; }
        public string unlock_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE unlock_op_type { get; set; }
        public CBigInt? unlock_value { get; set; }

        public SHOP_CATALOG_TYPE GetKey() => catalog_type;
    }

    /// <summary>SHOP_CATALOG_CHEST row</summary>
    public sealed class SHOP_CATALOG_CHEST : IEntityKey<int>
    {
        public int level { get; set; }
        public int max_exp { get; set; }
        public int ads_exp { get; set; }
        public int gain_exp01 { get; set; }
        public int gain_exp10 { get; set; }
        public string reward_ads { get; set; } = string.Empty;
        public string reward_paid01 { get; set; } = string.Empty;
        public string reward_paid10 { get; set; } = string.Empty;

        public int GetKey() => level;
    }

    /// <summary>SHOP_ITEM_DAILY row</summary>
    public sealed class SHOP_ITEM_DAILY : IEntityKey<string>
    {
        public string shop_item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public CURRENCY_TYPE currency_type { get; set; }
        public int unit_price { get; set; }
        public string reward_group_id { get; set; } = string.Empty;
        public int amount_min { get; set; }
        public int amount_max { get; set; }
        public int max_count { get; set; }
        public float select_rate { get; set; }
        public float discount_rate10_per { get; set; }
        public float discount_rate20_per { get; set; }
        public float discount_rate30_per { get; set; }
        public float discount_rate50_per { get; set; }

        public string GetKey() => shop_item_id;
    }

    /// <summary>SHOP_ITEM_EVENT row</summary>
    public sealed class SHOP_ITEM_EVENT : IEntityKey<string>
    {
        public string shop_item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public CURRENCY_TYPE currency_type { get; set; }
        public int price { get; set; }
        public string reward_group_id { get; set; } = string.Empty;
        public CDateTime start_time { get; set; }
        public CDateTime end_time { get; set; }

        public string GetKey() => shop_item_id;
    }

    /// <summary>SHOP_ITEM_CHEST row</summary>
    public sealed class SHOP_ITEM_CHEST : IEntityKey<string>
    {
        public string shop_item_id { get; set; } = string.Empty;
        public SHOP_PRODUCT_CHEST_TYPE chest_type { get; set; }
        public string name_id { get; set; } = string.Empty;
        public CURRENCY_TYPE currency_type { get; set; }
        public int price { get; set; }
        public int amount { get; set; }
        public int max_count { get; set; }

        public string GetKey() => shop_item_id;
    }

    /// <summary>SHOP_ITEM_PURCHASE row</summary>
    public sealed class SHOP_ITEM_PURCHASE : IEntityKey<string>
    {
        public string shop_item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string internal_product_id { get; set; } = string.Empty;
        public string season_id { get; set; } = string.Empty;

        public string GetKey() => shop_item_id;
    }

    /// <summary>SHOP_ITEM_GOLD row</summary>
    public sealed class SHOP_ITEM_GOLD : IEntityKey<string>
    {
        public string shop_item_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public CURRENCY_TYPE currency_type { get; set; }
        public int price { get; set; }
        public string reward_group_id { get; set; } = string.Empty;
        public int max_count { get; set; }

        public string GetKey() => shop_item_id;
    }

    /// <summary>PURCHASE row</summary>
    public sealed class PURCHASE : IEntityKey<string>
    {
        public string internal_product_id { get; set; } = string.Empty;
        public string reward_group_id { get; set; } = string.Empty;
        public PURCHASE_KIND kind { get; set; }
        public bool is_active { get; set; }
        public string store_sku_apple { get; set; } = string.Empty;
        public string store_sku_google { get; set; } = string.Empty;

        public string GetKey() => internal_product_id;
    }

    /// <summary>SKILL row</summary>
    public sealed class SKILL : IEntityKey<string>
    {
        public string skill_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public List<string> affect_list { get; set; } = new();

        public string GetKey() => skill_id;
    }

    /// <summary>PROJECTILE row</summary>
    public sealed class PROJECTILE : IEntityKey<string>
    {
        public string projectile_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public List<string> affect_list { get; set; } = new();

        public string GetKey() => projectile_id;
    }

    /// <summary>STATUS row</summary>
    public sealed class STATUS : IEntityKey<string>
    {
        public string status_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public List<string> affect_list { get; set; } = new();

        public string GetKey() => status_id;
    }

    /// <summary>AFFECT row</summary>
    public sealed class AFFECT : IEntityKey<string>
    {
        public string affect_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;

        public string GetKey() => affect_id;
    }

    /// <summary>TREASURE_CHEST row</summary>
    public sealed class TREASURE_CHEST : IEntityKey<int>
    {
        public int level { get; set; }
        public ITEM_GRADE_TYPE treasure_grade_type { get; set; }
        public int max_exp { get; set; }

        public int GetKey() => level;
    }

    /// <summary>TREASURE_REWARD row</summary>
    public sealed class TREASURE_REWARD : IEntityKey<int>
    {
        public int index { get; set; }
        public ITEM_GRADE_TYPE treasure_grade_type { get; set; }
        public int level { get; set; }
        public string condition_msg_id { get; set; } = string.Empty;
        public GAME_MESSAGE_OP_TYPE condition_op { get; set; }
        public CBigInt? condition_value { get; set; }
        public string reward_group_id { get; set; } = string.Empty;

        public int GetKey() => index;
    }

    /// <summary>UNIT_HERO row</summary>
    public sealed class UNIT_HERO : IEntityKey<string>
    {
        public string unit_id { get; set; } = string.Empty;
        public UNIT_HERO_TYPE hero_type { get; set; }
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => unit_id;
    }

    /// <summary>UNIT_HERO_LEVEL row</summary>
    public sealed class UNIT_HERO_LEVEL : IEntityKey<int>
    {
        public int index { get; set; }
        public string unit_id { get; set; } = string.Empty;
        public int unit_level { get; set; }
        public int max_hp { get; set; }

        public int GetKey() => index;
    }

    /// <summary>UNIT_MONSTER row</summary>
    public sealed class UNIT_MONSTER : IEntityKey<string>
    {
        public string unit_id { get; set; } = string.Empty;
        public string name_id { get; set; } = string.Empty;
        public string desc_id { get; set; } = string.Empty;

        public string GetKey() => unit_id;
    }

    /// <summary>UNIT_MONSTER_LEVEL row</summary>
    public sealed class UNIT_MONSTER_LEVEL : IEntityKey<int>
    {
        public int index { get; set; }
        public string unit_id { get; set; } = string.Empty;
        public int unit_level { get; set; }
        public int max_hp { get; set; }

        public int GetKey() => index;
    }

    // ================================================================
    // Table Containers
    // ================================================================

    /// <summary>TB_ACHIEVE_SOCIAL container</summary>
    public static partial class TB_ACHIEVE_SOCIAL
    {
        private static readonly Dictionary<int, ACHIEVE_SOCIAL> _dict = new();
        private static readonly List<ACHIEVE_SOCIAL> _list = new();

        private static readonly Dictionary<string, List<ACHIEVE_SOCIAL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ACHIEVE_SOCIAL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ACHIEVE_SOCIAL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ACHIEVE_SOCIAL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ACHIEVE_SOCIAL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ACHIEVE_SOCIAL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ACHIEVE_SOCIAL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.achieve_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ACHIEVE_SOCIAL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ACHIEVE_SOCIAL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ACHIEVE_SOCIAL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ACHIEVE_SOCIAL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ACHIEVE_PASS container</summary>
    public static partial class TB_ACHIEVE_PASS
    {
        private static readonly Dictionary<int, ACHIEVE_PASS> _dict = new();
        private static readonly List<ACHIEVE_PASS> _list = new();

        private static readonly Dictionary<string, List<ACHIEVE_PASS>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ACHIEVE_PASS> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ACHIEVE_PASS> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ACHIEVE_PASS>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ACHIEVE_PASS? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ACHIEVE_PASS? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ACHIEVE_PASS row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.achieve_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ACHIEVE_PASS>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ACHIEVE_PASS>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ACHIEVE_PASS>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ACHIEVE_PASS>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_REWARD container</summary>
    public static partial class TB_REWARD
    {
        private static readonly Dictionary<int, REWARD> _dict = new();
        private static readonly List<REWARD> _list = new();

        private static readonly Dictionary<string, List<REWARD>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<REWARD> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<REWARD> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<REWARD>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static REWARD? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out REWARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(REWARD row)
        {
            _list.Add(row);
            _dict[row.reward_num] = row;
            var groupKey = row.reward_group_id;
            _keyToGroup[row.reward_num] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<REWARD>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.reward_num, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.reward_num;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.reward_num;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<REWARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<REWARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<REWARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_GAME_ERROR container</summary>
    public static partial class TB_GAME_ERROR
    {
        private static readonly Dictionary<GAME_ERROR_TYPE, GAME_ERROR> _dict = new();
        private static readonly List<GAME_ERROR> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<GAME_ERROR> GetAll() => _list;

        public static GAME_ERROR? Get(GAME_ERROR_TYPE key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(GAME_ERROR_TYPE key, out GAME_ERROR? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        public static GAME_ERROR Find(GAME_ERROR_TYPE key)
        {
            if (_dict.TryGetValue(key, out var row)) return row;
            throw new KeyNotFoundException($"TB_GAME_ERROR: key {key} not found");
        }

        public static bool TryFind(GAME_ERROR_TYPE key, out GAME_ERROR? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(GAME_ERROR row)
        {
            _list.Add(row);
            _dict[row.id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<GAME_ERROR>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<GAME_ERROR>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<GAME_ERROR>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_GAME_MESSAGE container</summary>
    public static partial class TB_GAME_MESSAGE
    {
        private static readonly Dictionary<string, GAME_MESSAGE> _dict = new();
        private static readonly List<GAME_MESSAGE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<GAME_MESSAGE> GetAll() => _list;

        public static GAME_MESSAGE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out GAME_MESSAGE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(GAME_MESSAGE row)
        {
            _list.Add(row);
            _dict[row.message_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<GAME_MESSAGE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<GAME_MESSAGE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<GAME_MESSAGE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_HERO container</summary>
    public static partial class TB_ITEM_HERO
    {
        private static readonly Dictionary<string, ITEM_HERO> _dict = new();
        private static readonly List<ITEM_HERO> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_HERO> GetAll() => _list;

        public static ITEM_HERO? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_HERO? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_HERO row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_HERO>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_HERO>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_HERO>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_HERO_LEVEL container</summary>
    public static partial class TB_ITEM_HERO_LEVEL
    {
        private static readonly Dictionary<int, ITEM_HERO_LEVEL> _dict = new();
        private static readonly List<ITEM_HERO_LEVEL> _list = new();

        private static readonly Dictionary<string, List<ITEM_HERO_LEVEL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ITEM_HERO_LEVEL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ITEM_HERO_LEVEL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ITEM_HERO_LEVEL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ITEM_HERO_LEVEL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ITEM_HERO_LEVEL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_HERO_LEVEL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.item_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ITEM_HERO_LEVEL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_HERO_LEVEL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_HERO_LEVEL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_HERO_LEVEL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_EQUIP_SLOT container</summary>
    public static partial class TB_EQUIP_SLOT
    {
        private static readonly Dictionary<EQUIP_TYPE, EQUIP_SLOT> _dict = new();
        private static readonly List<EQUIP_SLOT> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<EQUIP_SLOT> GetAll() => _list;

        public static EQUIP_SLOT? Get(EQUIP_TYPE key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(EQUIP_TYPE key, out EQUIP_SLOT? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        public static EQUIP_SLOT Find(EQUIP_TYPE key)
        {
            if (_dict.TryGetValue(key, out var row)) return row;
            throw new KeyNotFoundException($"TB_EQUIP_SLOT: key {key} not found");
        }

        public static bool TryFind(EQUIP_TYPE key, out EQUIP_SLOT? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(EQUIP_SLOT row)
        {
            _list.Add(row);
            _dict[row.equip_type] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<EQUIP_SLOT>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<EQUIP_SLOT>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<EQUIP_SLOT>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_EQUIP container</summary>
    public static partial class TB_ITEM_EQUIP
    {
        private static readonly Dictionary<string, ITEM_EQUIP> _dict = new();
        private static readonly List<ITEM_EQUIP> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_EQUIP> GetAll() => _list;

        public static ITEM_EQUIP? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_EQUIP? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_EQUIP row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_EQUIP>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_EQUIP>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_EQUIP>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_EQUIP_LEVEL container</summary>
    public static partial class TB_ITEM_EQUIP_LEVEL
    {
        private static readonly Dictionary<int, ITEM_EQUIP_LEVEL> _dict = new();
        private static readonly List<ITEM_EQUIP_LEVEL> _list = new();

        private static readonly Dictionary<string, List<ITEM_EQUIP_LEVEL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ITEM_EQUIP_LEVEL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ITEM_EQUIP_LEVEL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ITEM_EQUIP_LEVEL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ITEM_EQUIP_LEVEL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ITEM_EQUIP_LEVEL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_EQUIP_LEVEL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.item_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ITEM_EQUIP_LEVEL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_EQUIP_LEVEL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_EQUIP_LEVEL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_EQUIP_LEVEL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_CARD container</summary>
    public static partial class TB_ITEM_CARD
    {
        private static readonly Dictionary<string, ITEM_CARD> _dict = new();
        private static readonly List<ITEM_CARD> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_CARD> GetAll() => _list;

        public static ITEM_CARD? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_CARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_CARD row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_CARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_CARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_CARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_CARD_LEVEL container</summary>
    public static partial class TB_ITEM_CARD_LEVEL
    {
        private static readonly Dictionary<int, ITEM_CARD_LEVEL> _dict = new();
        private static readonly List<ITEM_CARD_LEVEL> _list = new();

        private static readonly Dictionary<string, List<ITEM_CARD_LEVEL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ITEM_CARD_LEVEL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ITEM_CARD_LEVEL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ITEM_CARD_LEVEL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ITEM_CARD_LEVEL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ITEM_CARD_LEVEL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_CARD_LEVEL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.item_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ITEM_CARD_LEVEL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_CARD_LEVEL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_CARD_LEVEL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_CARD_LEVEL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_MATERIAL container</summary>
    public static partial class TB_ITEM_MATERIAL
    {
        private static readonly Dictionary<string, ITEM_MATERIAL> _dict = new();
        private static readonly List<ITEM_MATERIAL> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_MATERIAL> GetAll() => _list;

        public static ITEM_MATERIAL? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_MATERIAL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_MATERIAL row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_MATERIAL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_MATERIAL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_MATERIAL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_RENTAL container</summary>
    public static partial class TB_ITEM_RENTAL
    {
        private static readonly Dictionary<string, ITEM_RENTAL> _dict = new();
        private static readonly List<ITEM_RENTAL> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_RENTAL> GetAll() => _list;

        public static ITEM_RENTAL? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_RENTAL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_RENTAL row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_RENTAL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_RENTAL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_RENTAL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_PASS container</summary>
    public static partial class TB_ITEM_PASS
    {
        private static readonly Dictionary<string, ITEM_PASS> _dict = new();
        private static readonly List<ITEM_PASS> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ITEM_PASS> GetAll() => _list;

        public static ITEM_PASS? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_PASS? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_PASS row)
        {
            _list.Add(row);
            _dict[row.item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_PASS>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_PASS>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_PASS>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ITEM_TREASURE container</summary>
    public static partial class TB_ITEM_TREASURE
    {
        private static readonly Dictionary<string, ITEM_TREASURE> _dict = new();
        private static readonly List<ITEM_TREASURE> _list = new();

        private static readonly Dictionary<string, List<ITEM_TREASURE>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, string> _groupPrimaryKey = new();
        private static readonly Dictionary<string, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ITEM_TREASURE> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ITEM_TREASURE> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ITEM_TREASURE>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out string key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(string key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ITEM_TREASURE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ITEM_TREASURE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ITEM_TREASURE row)
        {
            _list.Add(row);
            _dict[row.chest_num] = row;
            var groupKey = row.item_id;
            _keyToGroup[row.chest_num] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ITEM_TREASURE>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<string>.Default.Compare(row.chest_num, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.chest_num;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.chest_num;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ITEM_TREASURE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ITEM_TREASURE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ITEM_TREASURE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ADVERTISE container</summary>
    public static partial class TB_ADVERTISE
    {
        private static readonly Dictionary<string, ADVERTISE> _dict = new();
        private static readonly List<ADVERTISE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ADVERTISE> GetAll() => _list;

        public static ADVERTISE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ADVERTISE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ADVERTISE row)
        {
            _list.Add(row);
            _dict[row.advertise_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ADVERTISE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ADVERTISE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ADVERTISE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ATTEND container</summary>
    public static partial class TB_ATTEND
    {
        private static readonly Dictionary<string, ATTEND> _dict = new();
        private static readonly List<ATTEND> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ATTEND> GetAll() => _list;

        public static ATTEND? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ATTEND? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ATTEND row)
        {
            _list.Add(row);
            _dict[row.attend_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ATTEND>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ATTEND>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ATTEND>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_LEADERBOARD container</summary>
    public static partial class TB_LEADERBOARD
    {
        private static readonly Dictionary<string, LEADERBOARD> _dict = new();
        private static readonly List<LEADERBOARD> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<LEADERBOARD> GetAll() => _list;

        public static LEADERBOARD? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out LEADERBOARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(LEADERBOARD row)
        {
            _list.Add(row);
            _dict[row.leaderboard_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<LEADERBOARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<LEADERBOARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<LEADERBOARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_LEADERBOARD_REWARD container</summary>
    public static partial class TB_LEADERBOARD_REWARD
    {
        private static readonly Dictionary<int, LEADERBOARD_REWARD> _dict = new();
        private static readonly List<LEADERBOARD_REWARD> _list = new();

        private static readonly Dictionary<string, List<LEADERBOARD_REWARD>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<LEADERBOARD_REWARD> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<LEADERBOARD_REWARD> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<LEADERBOARD_REWARD>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static LEADERBOARD_REWARD? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out LEADERBOARD_REWARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(LEADERBOARD_REWARD row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.leaderboard_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<LEADERBOARD_REWARD>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<LEADERBOARD_REWARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<LEADERBOARD_REWARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<LEADERBOARD_REWARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SEASON container</summary>
    public static partial class TB_SEASON
    {
        private static readonly Dictionary<string, SEASON> _dict = new();
        private static readonly List<SEASON> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SEASON> GetAll() => _list;

        public static SEASON? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SEASON? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SEASON row)
        {
            _list.Add(row);
            _dict[row.season_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SEASON>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SEASON>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SEASON>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_MISSION_DAILY container</summary>
    public static partial class TB_MISSION_DAILY
    {
        private static readonly Dictionary<string, MISSION_DAILY> _dict = new();
        private static readonly List<MISSION_DAILY> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<MISSION_DAILY> GetAll() => _list;

        public static MISSION_DAILY? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out MISSION_DAILY? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(MISSION_DAILY row)
        {
            _list.Add(row);
            _dict[row.mission_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<MISSION_DAILY>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<MISSION_DAILY>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<MISSION_DAILY>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_MISSION_WEEKLY container</summary>
    public static partial class TB_MISSION_WEEKLY
    {
        private static readonly Dictionary<string, MISSION_WEEKLY> _dict = new();
        private static readonly List<MISSION_WEEKLY> _list = new();

        private static readonly Dictionary<int, List<MISSION_WEEKLY>> _groupDict = new();
        private static readonly List<int> _groupList = new();
        private static readonly Dictionary<int, string> _groupPrimaryKey = new();
        private static readonly Dictionary<string, int> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<MISSION_WEEKLY> GetAll() => _list;

        public static IReadOnlyList<int> GetGroupKeys() => _groupList;

        public static IReadOnlyList<MISSION_WEEKLY> GetByGroup(int groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<MISSION_WEEKLY>();
        }

        public static bool TryGetGroupPrimaryKey(int groupKey, out string key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(string key, out int groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static MISSION_WEEKLY? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out MISSION_WEEKLY? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(MISSION_WEEKLY row)
        {
            _list.Add(row);
            _dict[row.mission_id] = row;
            var groupKey = row.day;
            _keyToGroup[row.mission_id] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<MISSION_WEEKLY>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<string>.Default.Compare(row.mission_id, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.mission_id;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.mission_id;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<MISSION_WEEKLY>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<MISSION_WEEKLY>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<MISSION_WEEKLY>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_CATALOG container</summary>
    public static partial class TB_SHOP_CATALOG
    {
        private static readonly Dictionary<SHOP_CATALOG_TYPE, SHOP_CATALOG> _dict = new();
        private static readonly List<SHOP_CATALOG> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_CATALOG> GetAll() => _list;

        public static SHOP_CATALOG? Get(SHOP_CATALOG_TYPE key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(SHOP_CATALOG_TYPE key, out SHOP_CATALOG? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_CATALOG row)
        {
            _list.Add(row);
            _dict[row.catalog_type] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_CATALOG>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_CATALOG>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_CATALOG>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_CATALOG_CHEST container</summary>
    public static partial class TB_SHOP_CATALOG_CHEST
    {
        private static readonly Dictionary<int, SHOP_CATALOG_CHEST> _dict = new();
        private static readonly List<SHOP_CATALOG_CHEST> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_CATALOG_CHEST> GetAll() => _list;

        public static SHOP_CATALOG_CHEST? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out SHOP_CATALOG_CHEST? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_CATALOG_CHEST row)
        {
            _list.Add(row);
            _dict[row.level] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_CATALOG_CHEST>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_CATALOG_CHEST>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_CATALOG_CHEST>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_ITEM_DAILY container</summary>
    public static partial class TB_SHOP_ITEM_DAILY
    {
        private static readonly Dictionary<string, SHOP_ITEM_DAILY> _dict = new();
        private static readonly List<SHOP_ITEM_DAILY> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_ITEM_DAILY> GetAll() => _list;

        public static SHOP_ITEM_DAILY? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SHOP_ITEM_DAILY? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_ITEM_DAILY row)
        {
            _list.Add(row);
            _dict[row.shop_item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_ITEM_DAILY>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_DAILY>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_DAILY>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_ITEM_EVENT container</summary>
    public static partial class TB_SHOP_ITEM_EVENT
    {
        private static readonly Dictionary<string, SHOP_ITEM_EVENT> _dict = new();
        private static readonly List<SHOP_ITEM_EVENT> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_ITEM_EVENT> GetAll() => _list;

        public static SHOP_ITEM_EVENT? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SHOP_ITEM_EVENT? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_ITEM_EVENT row)
        {
            _list.Add(row);
            _dict[row.shop_item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_ITEM_EVENT>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_EVENT>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_EVENT>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_ITEM_CHEST container</summary>
    public static partial class TB_SHOP_ITEM_CHEST
    {
        private static readonly Dictionary<string, SHOP_ITEM_CHEST> _dict = new();
        private static readonly List<SHOP_ITEM_CHEST> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_ITEM_CHEST> GetAll() => _list;

        public static SHOP_ITEM_CHEST? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SHOP_ITEM_CHEST? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_ITEM_CHEST row)
        {
            _list.Add(row);
            _dict[row.shop_item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_ITEM_CHEST>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_CHEST>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_CHEST>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_ITEM_PURCHASE container</summary>
    public static partial class TB_SHOP_ITEM_PURCHASE
    {
        private static readonly Dictionary<string, SHOP_ITEM_PURCHASE> _dict = new();
        private static readonly List<SHOP_ITEM_PURCHASE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_ITEM_PURCHASE> GetAll() => _list;

        public static SHOP_ITEM_PURCHASE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SHOP_ITEM_PURCHASE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_ITEM_PURCHASE row)
        {
            _list.Add(row);
            _dict[row.shop_item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_ITEM_PURCHASE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_PURCHASE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_PURCHASE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SHOP_ITEM_GOLD container</summary>
    public static partial class TB_SHOP_ITEM_GOLD
    {
        private static readonly Dictionary<string, SHOP_ITEM_GOLD> _dict = new();
        private static readonly List<SHOP_ITEM_GOLD> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SHOP_ITEM_GOLD> GetAll() => _list;

        public static SHOP_ITEM_GOLD? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SHOP_ITEM_GOLD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SHOP_ITEM_GOLD row)
        {
            _list.Add(row);
            _dict[row.shop_item_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SHOP_ITEM_GOLD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_GOLD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SHOP_ITEM_GOLD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_PURCHASE container</summary>
    public static partial class TB_PURCHASE
    {
        private static readonly Dictionary<string, PURCHASE> _dict = new();
        private static readonly List<PURCHASE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<PURCHASE> GetAll() => _list;

        public static PURCHASE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out PURCHASE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(PURCHASE row)
        {
            _list.Add(row);
            _dict[row.internal_product_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<PURCHASE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<PURCHASE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<PURCHASE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_SKILL container</summary>
    public static partial class TB_SKILL
    {
        private static readonly Dictionary<string, SKILL> _dict = new();
        private static readonly List<SKILL> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<SKILL> GetAll() => _list;

        public static SKILL? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out SKILL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(SKILL row)
        {
            _list.Add(row);
            _dict[row.skill_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<SKILL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<SKILL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<SKILL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_PROJECTILE container</summary>
    public static partial class TB_PROJECTILE
    {
        private static readonly Dictionary<string, PROJECTILE> _dict = new();
        private static readonly List<PROJECTILE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<PROJECTILE> GetAll() => _list;

        public static PROJECTILE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out PROJECTILE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(PROJECTILE row)
        {
            _list.Add(row);
            _dict[row.projectile_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<PROJECTILE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<PROJECTILE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<PROJECTILE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_STATUS container</summary>
    public static partial class TB_STATUS
    {
        private static readonly Dictionary<string, STATUS> _dict = new();
        private static readonly List<STATUS> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<STATUS> GetAll() => _list;

        public static STATUS? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out STATUS? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(STATUS row)
        {
            _list.Add(row);
            _dict[row.status_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<STATUS>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<STATUS>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<STATUS>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_AFFECT container</summary>
    public static partial class TB_AFFECT
    {
        private static readonly Dictionary<string, AFFECT> _dict = new();
        private static readonly List<AFFECT> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<AFFECT> GetAll() => _list;

        public static AFFECT? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out AFFECT? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(AFFECT row)
        {
            _list.Add(row);
            _dict[row.affect_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<AFFECT>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<AFFECT>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<AFFECT>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_TREASURE_CHEST container</summary>
    public static partial class TB_TREASURE_CHEST
    {
        private static readonly Dictionary<int, TREASURE_CHEST> _dict = new();
        private static readonly List<TREASURE_CHEST> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<TREASURE_CHEST> GetAll() => _list;

        public static TREASURE_CHEST? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out TREASURE_CHEST? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(TREASURE_CHEST row)
        {
            _list.Add(row);
            _dict[row.level] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<TREASURE_CHEST>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<TREASURE_CHEST>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<TREASURE_CHEST>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_TREASURE_REWARD container</summary>
    public static partial class TB_TREASURE_REWARD
    {
        private static readonly Dictionary<int, TREASURE_REWARD> _dict = new();
        private static readonly List<TREASURE_REWARD> _list = new();

        private static readonly Dictionary<ITEM_GRADE_TYPE, List<TREASURE_REWARD>> _groupDict = new();
        private static readonly List<ITEM_GRADE_TYPE> _groupList = new();
        private static readonly Dictionary<ITEM_GRADE_TYPE, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, ITEM_GRADE_TYPE> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<TREASURE_REWARD> GetAll() => _list;

        public static IReadOnlyList<ITEM_GRADE_TYPE> GetGroupKeys() => _groupList;

        public static IReadOnlyList<TREASURE_REWARD> GetByGroup(ITEM_GRADE_TYPE groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<TREASURE_REWARD>();
        }

        public static bool TryGetGroupPrimaryKey(ITEM_GRADE_TYPE groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out ITEM_GRADE_TYPE groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static TREASURE_REWARD? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out TREASURE_REWARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(TREASURE_REWARD row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.treasure_grade_type;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<TREASURE_REWARD>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<TREASURE_REWARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<TREASURE_REWARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<TREASURE_REWARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_HERO container</summary>
    public static partial class TB_UNIT_HERO
    {
        private static readonly Dictionary<string, UNIT_HERO> _dict = new();
        private static readonly List<UNIT_HERO> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<UNIT_HERO> GetAll() => _list;

        public static UNIT_HERO? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out UNIT_HERO? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_HERO row)
        {
            _list.Add(row);
            _dict[row.unit_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_HERO>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_HERO>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_HERO>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_HERO_LEVEL container</summary>
    public static partial class TB_UNIT_HERO_LEVEL
    {
        private static readonly Dictionary<int, UNIT_HERO_LEVEL> _dict = new();
        private static readonly List<UNIT_HERO_LEVEL> _list = new();

        private static readonly Dictionary<string, List<UNIT_HERO_LEVEL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<UNIT_HERO_LEVEL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<UNIT_HERO_LEVEL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<UNIT_HERO_LEVEL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static UNIT_HERO_LEVEL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out UNIT_HERO_LEVEL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_HERO_LEVEL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.unit_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<UNIT_HERO_LEVEL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_HERO_LEVEL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_HERO_LEVEL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_HERO_LEVEL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_MONSTER container</summary>
    public static partial class TB_UNIT_MONSTER
    {
        private static readonly Dictionary<string, UNIT_MONSTER> _dict = new();
        private static readonly List<UNIT_MONSTER> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<UNIT_MONSTER> GetAll() => _list;

        public static UNIT_MONSTER? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out UNIT_MONSTER? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_MONSTER row)
        {
            _list.Add(row);
            _dict[row.unit_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_MONSTER>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_MONSTER_LEVEL container</summary>
    public static partial class TB_UNIT_MONSTER_LEVEL
    {
        private static readonly Dictionary<int, UNIT_MONSTER_LEVEL> _dict = new();
        private static readonly List<UNIT_MONSTER_LEVEL> _list = new();

        private static readonly Dictionary<string, List<UNIT_MONSTER_LEVEL>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<UNIT_MONSTER_LEVEL> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<UNIT_MONSTER_LEVEL> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<UNIT_MONSTER_LEVEL>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static UNIT_MONSTER_LEVEL? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out UNIT_MONSTER_LEVEL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_MONSTER_LEVEL row)
        {
            _list.Add(row);
            _dict[row.index] = row;
            var groupKey = row.unit_id;
            _keyToGroup[row.index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<UNIT_MONSTER_LEVEL>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_MONSTER_LEVEL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER_LEVEL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER_LEVEL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    // ================================================================
    // Table ID Types (for Inspector binding)
    // ================================================================

    /// <summary>Inspector-bindable ID for ACHIEVE_SOCIAL</summary>
    [Serializable]
    public sealed class ACHIEVE_SOCIAL_ID
    {
        public int Value;

        public static implicit operator int(ACHIEVE_SOCIAL_ID id) => id.Value;
        public static implicit operator ACHIEVE_SOCIAL_ID(int value) => new ACHIEVE_SOCIAL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ACHIEVE_PASS</summary>
    [Serializable]
    public sealed class ACHIEVE_PASS_ID
    {
        public int Value;

        public static implicit operator int(ACHIEVE_PASS_ID id) => id.Value;
        public static implicit operator ACHIEVE_PASS_ID(int value) => new ACHIEVE_PASS_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for REWARD</summary>
    [Serializable]
    public sealed class REWARD_ID
    {
        public int Value;

        public static implicit operator int(REWARD_ID id) => id.Value;
        public static implicit operator REWARD_ID(int value) => new REWARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for GAME_ERROR</summary>
    [Serializable]
    public sealed class GAME_ERROR_ID
    {
        public GAME_ERROR_TYPE Value;

        public static implicit operator GAME_ERROR_TYPE(GAME_ERROR_ID id) => id.Value;
        public static implicit operator GAME_ERROR_ID(GAME_ERROR_TYPE value) => new GAME_ERROR_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for GAME_MESSAGE</summary>
    [Serializable]
    public sealed class GAME_MESSAGE_ID
    {
        public string Value;

        public static implicit operator string(GAME_MESSAGE_ID id) => id.Value;
        public static implicit operator GAME_MESSAGE_ID(string value) => new GAME_MESSAGE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_HERO</summary>
    [Serializable]
    public sealed class ITEM_HERO_ID
    {
        public string Value;

        public static implicit operator string(ITEM_HERO_ID id) => id.Value;
        public static implicit operator ITEM_HERO_ID(string value) => new ITEM_HERO_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_HERO_LEVEL</summary>
    [Serializable]
    public sealed class ITEM_HERO_LEVEL_ID
    {
        public int Value;

        public static implicit operator int(ITEM_HERO_LEVEL_ID id) => id.Value;
        public static implicit operator ITEM_HERO_LEVEL_ID(int value) => new ITEM_HERO_LEVEL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for EQUIP_SLOT</summary>
    [Serializable]
    public sealed class EQUIP_SLOT_ID
    {
        public EQUIP_TYPE Value;

        public static implicit operator EQUIP_TYPE(EQUIP_SLOT_ID id) => id.Value;
        public static implicit operator EQUIP_SLOT_ID(EQUIP_TYPE value) => new EQUIP_SLOT_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_EQUIP</summary>
    [Serializable]
    public sealed class ITEM_EQUIP_ID
    {
        public string Value;

        public static implicit operator string(ITEM_EQUIP_ID id) => id.Value;
        public static implicit operator ITEM_EQUIP_ID(string value) => new ITEM_EQUIP_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_EQUIP_LEVEL</summary>
    [Serializable]
    public sealed class ITEM_EQUIP_LEVEL_ID
    {
        public int Value;

        public static implicit operator int(ITEM_EQUIP_LEVEL_ID id) => id.Value;
        public static implicit operator ITEM_EQUIP_LEVEL_ID(int value) => new ITEM_EQUIP_LEVEL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_CARD</summary>
    [Serializable]
    public sealed class ITEM_CARD_ID
    {
        public string Value;

        public static implicit operator string(ITEM_CARD_ID id) => id.Value;
        public static implicit operator ITEM_CARD_ID(string value) => new ITEM_CARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_CARD_LEVEL</summary>
    [Serializable]
    public sealed class ITEM_CARD_LEVEL_ID
    {
        public int Value;

        public static implicit operator int(ITEM_CARD_LEVEL_ID id) => id.Value;
        public static implicit operator ITEM_CARD_LEVEL_ID(int value) => new ITEM_CARD_LEVEL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_MATERIAL</summary>
    [Serializable]
    public sealed class ITEM_MATERIAL_ID
    {
        public string Value;

        public static implicit operator string(ITEM_MATERIAL_ID id) => id.Value;
        public static implicit operator ITEM_MATERIAL_ID(string value) => new ITEM_MATERIAL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_RENTAL</summary>
    [Serializable]
    public sealed class ITEM_RENTAL_ID
    {
        public string Value;

        public static implicit operator string(ITEM_RENTAL_ID id) => id.Value;
        public static implicit operator ITEM_RENTAL_ID(string value) => new ITEM_RENTAL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_PASS</summary>
    [Serializable]
    public sealed class ITEM_PASS_ID
    {
        public string Value;

        public static implicit operator string(ITEM_PASS_ID id) => id.Value;
        public static implicit operator ITEM_PASS_ID(string value) => new ITEM_PASS_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ITEM_TREASURE</summary>
    [Serializable]
    public sealed class ITEM_TREASURE_ID
    {
        public string Value;

        public static implicit operator string(ITEM_TREASURE_ID id) => id.Value;
        public static implicit operator ITEM_TREASURE_ID(string value) => new ITEM_TREASURE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ADVERTISE</summary>
    [Serializable]
    public sealed class ADVERTISE_ID
    {
        public string Value;

        public static implicit operator string(ADVERTISE_ID id) => id.Value;
        public static implicit operator ADVERTISE_ID(string value) => new ADVERTISE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ATTEND</summary>
    [Serializable]
    public sealed class ATTEND_ID
    {
        public string Value;

        public static implicit operator string(ATTEND_ID id) => id.Value;
        public static implicit operator ATTEND_ID(string value) => new ATTEND_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for LEADERBOARD</summary>
    [Serializable]
    public sealed class LEADERBOARD_ID
    {
        public string Value;

        public static implicit operator string(LEADERBOARD_ID id) => id.Value;
        public static implicit operator LEADERBOARD_ID(string value) => new LEADERBOARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for LEADERBOARD_REWARD</summary>
    [Serializable]
    public sealed class LEADERBOARD_REWARD_ID
    {
        public int Value;

        public static implicit operator int(LEADERBOARD_REWARD_ID id) => id.Value;
        public static implicit operator LEADERBOARD_REWARD_ID(int value) => new LEADERBOARD_REWARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SEASON</summary>
    [Serializable]
    public sealed class SEASON_ID
    {
        public string Value;

        public static implicit operator string(SEASON_ID id) => id.Value;
        public static implicit operator SEASON_ID(string value) => new SEASON_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for MISSION_DAILY</summary>
    [Serializable]
    public sealed class MISSION_DAILY_ID
    {
        public string Value;

        public static implicit operator string(MISSION_DAILY_ID id) => id.Value;
        public static implicit operator MISSION_DAILY_ID(string value) => new MISSION_DAILY_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for MISSION_WEEKLY</summary>
    [Serializable]
    public sealed class MISSION_WEEKLY_ID
    {
        public string Value;

        public static implicit operator string(MISSION_WEEKLY_ID id) => id.Value;
        public static implicit operator MISSION_WEEKLY_ID(string value) => new MISSION_WEEKLY_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_CATALOG</summary>
    [Serializable]
    public sealed class SHOP_CATALOG_ID
    {
        public SHOP_CATALOG_TYPE Value;

        public static implicit operator SHOP_CATALOG_TYPE(SHOP_CATALOG_ID id) => id.Value;
        public static implicit operator SHOP_CATALOG_ID(SHOP_CATALOG_TYPE value) => new SHOP_CATALOG_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_CATALOG_CHEST</summary>
    [Serializable]
    public sealed class SHOP_CATALOG_CHEST_ID
    {
        public int Value;

        public static implicit operator int(SHOP_CATALOG_CHEST_ID id) => id.Value;
        public static implicit operator SHOP_CATALOG_CHEST_ID(int value) => new SHOP_CATALOG_CHEST_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_ITEM_DAILY</summary>
    [Serializable]
    public sealed class SHOP_ITEM_DAILY_ID
    {
        public string Value;

        public static implicit operator string(SHOP_ITEM_DAILY_ID id) => id.Value;
        public static implicit operator SHOP_ITEM_DAILY_ID(string value) => new SHOP_ITEM_DAILY_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_ITEM_EVENT</summary>
    [Serializable]
    public sealed class SHOP_ITEM_EVENT_ID
    {
        public string Value;

        public static implicit operator string(SHOP_ITEM_EVENT_ID id) => id.Value;
        public static implicit operator SHOP_ITEM_EVENT_ID(string value) => new SHOP_ITEM_EVENT_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_ITEM_CHEST</summary>
    [Serializable]
    public sealed class SHOP_ITEM_CHEST_ID
    {
        public string Value;

        public static implicit operator string(SHOP_ITEM_CHEST_ID id) => id.Value;
        public static implicit operator SHOP_ITEM_CHEST_ID(string value) => new SHOP_ITEM_CHEST_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_ITEM_PURCHASE</summary>
    [Serializable]
    public sealed class SHOP_ITEM_PURCHASE_ID
    {
        public string Value;

        public static implicit operator string(SHOP_ITEM_PURCHASE_ID id) => id.Value;
        public static implicit operator SHOP_ITEM_PURCHASE_ID(string value) => new SHOP_ITEM_PURCHASE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SHOP_ITEM_GOLD</summary>
    [Serializable]
    public sealed class SHOP_ITEM_GOLD_ID
    {
        public string Value;

        public static implicit operator string(SHOP_ITEM_GOLD_ID id) => id.Value;
        public static implicit operator SHOP_ITEM_GOLD_ID(string value) => new SHOP_ITEM_GOLD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for PURCHASE</summary>
    [Serializable]
    public sealed class PURCHASE_ID
    {
        public string Value;

        public static implicit operator string(PURCHASE_ID id) => id.Value;
        public static implicit operator PURCHASE_ID(string value) => new PURCHASE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for SKILL</summary>
    [Serializable]
    public sealed class SKILL_ID
    {
        public string Value;

        public static implicit operator string(SKILL_ID id) => id.Value;
        public static implicit operator SKILL_ID(string value) => new SKILL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for PROJECTILE</summary>
    [Serializable]
    public sealed class PROJECTILE_ID
    {
        public string Value;

        public static implicit operator string(PROJECTILE_ID id) => id.Value;
        public static implicit operator PROJECTILE_ID(string value) => new PROJECTILE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for STATUS</summary>
    [Serializable]
    public sealed class STATUS_ID
    {
        public string Value;

        public static implicit operator string(STATUS_ID id) => id.Value;
        public static implicit operator STATUS_ID(string value) => new STATUS_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for AFFECT</summary>
    [Serializable]
    public sealed class AFFECT_ID
    {
        public string Value;

        public static implicit operator string(AFFECT_ID id) => id.Value;
        public static implicit operator AFFECT_ID(string value) => new AFFECT_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for TREASURE_CHEST</summary>
    [Serializable]
    public sealed class TREASURE_CHEST_ID
    {
        public int Value;

        public static implicit operator int(TREASURE_CHEST_ID id) => id.Value;
        public static implicit operator TREASURE_CHEST_ID(int value) => new TREASURE_CHEST_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for TREASURE_REWARD</summary>
    [Serializable]
    public sealed class TREASURE_REWARD_ID
    {
        public int Value;

        public static implicit operator int(TREASURE_REWARD_ID id) => id.Value;
        public static implicit operator TREASURE_REWARD_ID(int value) => new TREASURE_REWARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_HERO</summary>
    [Serializable]
    public sealed class UNIT_HERO_ID
    {
        public string Value;

        public static implicit operator string(UNIT_HERO_ID id) => id.Value;
        public static implicit operator UNIT_HERO_ID(string value) => new UNIT_HERO_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_HERO_LEVEL</summary>
    [Serializable]
    public sealed class UNIT_HERO_LEVEL_ID
    {
        public int Value;

        public static implicit operator int(UNIT_HERO_LEVEL_ID id) => id.Value;
        public static implicit operator UNIT_HERO_LEVEL_ID(int value) => new UNIT_HERO_LEVEL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_MONSTER</summary>
    [Serializable]
    public sealed class UNIT_MONSTER_ID
    {
        public string Value;

        public static implicit operator string(UNIT_MONSTER_ID id) => id.Value;
        public static implicit operator UNIT_MONSTER_ID(string value) => new UNIT_MONSTER_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_MONSTER_LEVEL</summary>
    [Serializable]
    public sealed class UNIT_MONSTER_LEVEL_ID
    {
        public int Value;

        public static implicit operator int(UNIT_MONSTER_LEVEL_ID id) => id.Value;
        public static implicit operator UNIT_MONSTER_LEVEL_ID(int value) => new UNIT_MONSTER_LEVEL_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for string table GAME_TEXT</summary>
    [Serializable]
    public sealed class GAME_TEXT_ID
    {
        public string Value = string.Empty;

        public static implicit operator string(GAME_TEXT_ID id) => id.Value;
        public static implicit operator GAME_TEXT_ID(string value) => new GAME_TEXT_ID { Value = value };
    }

    /// <summary>Table ID validation extensions</summary>
    public static class TableIdExtensions
    {
        public static bool IsValid(this ACHIEVE_SOCIAL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this ACHIEVE_PASS_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this REWARD_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this GAME_ERROR_ID? obj) => obj != null && !EqualityComparer<GAME_ERROR_TYPE>.Default.Equals(obj.Value, default);
        public static bool IsValid(this GAME_MESSAGE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_HERO_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_HERO_LEVEL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this EQUIP_SLOT_ID? obj) => obj != null && !EqualityComparer<EQUIP_TYPE>.Default.Equals(obj.Value, default);
        public static bool IsValid(this ITEM_EQUIP_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_EQUIP_LEVEL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this ITEM_CARD_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_CARD_LEVEL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this ITEM_MATERIAL_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_RENTAL_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_PASS_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ITEM_TREASURE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ADVERTISE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ATTEND_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this LEADERBOARD_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this LEADERBOARD_REWARD_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this SEASON_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this MISSION_DAILY_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this MISSION_WEEKLY_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SHOP_CATALOG_ID? obj) => obj != null && !EqualityComparer<SHOP_CATALOG_TYPE>.Default.Equals(obj.Value, default);
        public static bool IsValid(this SHOP_CATALOG_CHEST_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this SHOP_ITEM_DAILY_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SHOP_ITEM_EVENT_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SHOP_ITEM_CHEST_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SHOP_ITEM_PURCHASE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SHOP_ITEM_GOLD_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this PURCHASE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this SKILL_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this PROJECTILE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this STATUS_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this AFFECT_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this TREASURE_CHEST_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this TREASURE_REWARD_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this UNIT_HERO_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this UNIT_HERO_LEVEL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this UNIT_MONSTER_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this UNIT_MONSTER_LEVEL_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this GAME_TEXT_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
    }

}