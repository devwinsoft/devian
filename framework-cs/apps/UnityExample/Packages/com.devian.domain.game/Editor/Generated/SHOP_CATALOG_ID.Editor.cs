// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#if UNITY_EDITOR

using UnityEditor;
using UnityEngine;
using Devian.Domain.Game;

namespace Devian
{
    /// <summary>Selector for SHOP_CATALOG_ID</summary>
    public class Game_SHOP_CATALOG_ID_Selector : BaseEditorID_Selector
    {
        protected override string GetDisplayTypeName() => "SHOP_CATALOG";

        public override void Reload()
        {
            ClearItems();
            TB_SHOP_CATALOG.Clear();

            var textAssets = AssetManager.FindAssets<TextAsset>("SHOP_CATALOG");
            foreach (var ta in textAssets)
            {
                var assetPath = AssetDatabase.GetAssetPath(ta);
                if (!assetPath.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase))
                    continue;

                TB_SHOP_CATALOG.LoadFromNdjson(ta.text);
                break;
            }

            foreach (var row in TB_SHOP_CATALOG.GetAll())
            {
                AddItem(row.GetKey().ToString());
            }
        }
    }

    /// <summary>PropertyDrawer for SHOP_CATALOG_ID</summary>
    [CustomPropertyDrawer(typeof(SHOP_CATALOG_ID))]
    public class Game_SHOP_CATALOG_ID_Drawer : BaseEditorID_Drawer<Game_SHOP_CATALOG_ID_Selector>
    {
        protected override Game_SHOP_CATALOG_ID_Selector GetSelector()
        {
            var w = ScriptableObject.CreateInstance<Game_SHOP_CATALOG_ID_Selector>();
            w.titleContent = new GUIContent("Select SHOP_CATALOG");
            w.ShowUtility();
            return w;
        }
    }
}

#endif