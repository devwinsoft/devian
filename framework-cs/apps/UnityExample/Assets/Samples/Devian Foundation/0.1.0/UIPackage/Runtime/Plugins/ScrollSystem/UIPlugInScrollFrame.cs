// <auto-generated>
// Unity ScrollSystem - UIPlugInScrollFrame
// SSOT: skills/devian-unity/23-ui-package/35-ui-plugin-scroll-system/SKILL.md
// </auto-generated>

using System;
using UnityEngine;

namespace Devian
{
    /// <summary>
    /// 고정 프리팹 섹션 (배너, 헤더, 구분선 등).
    /// Content 자식으로 Prefab에 배치한다.
    /// Pool 대상이 아니며, 뷰포트 밖이면 SetActive(false) 처리.
    /// Frame GameObject 자체가 표시 요소이다.
    /// </summary>
    public class UIPlugInScrollFrame : MonoBehaviour
    {
        // ─── Inspector Configuration ───

        [SerializeField] private float _height = 100f;

        // ─── Properties ───

        public float Height
        {
            get => _height;
            set => _height = value;
        }

        // ─── Callbacks ───

        /// <summary>뷰포트에 들어올 때 호출.</summary>
        public Action<UIPlugInScrollFrame> onBind;

        /// <summary>뷰포트에서 나갈 때 호출.</summary>
        public Action<UIPlugInScrollFrame> onUnbind;

        // ─── Internal (Container가 Init 시 설정) ───

        internal int SectionIndex;
        internal int LayoutRowIndex;
        internal bool IsActive;
    }
}
