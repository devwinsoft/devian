// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#if UNITY_EDITOR

using UnityEditor;
using UnityEngine;
using Devian.Domain.Game;

namespace Devian
{
    /// <summary>Selector for SHOP_ITEM_GOLD_ID</summary>
    public class Game_SHOP_ITEM_GOLD_ID_Selector : BaseEditorID_Selector
    {
        protected override string GetDisplayTypeName() => "SHOP_ITEM_GOLD";

        public override void Reload()
        {
            ClearItems();
            TB_SHOP_ITEM_GOLD.Clear();

            var textAssets = AssetManager.FindAssets<TextAsset>("SHOP_ITEM_GOLD");
            foreach (var ta in textAssets)
            {
                var assetPath = AssetDatabase.GetAssetPath(ta);
                if (!assetPath.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase))
                    continue;

                TB_SHOP_ITEM_GOLD.LoadFromNdjson(ta.text);
                break;
            }

            foreach (var row in TB_SHOP_ITEM_GOLD.GetAll())
            {
                AddItem(row.GetKey().ToString());
            }
        }
    }

    /// <summary>PropertyDrawer for SHOP_ITEM_GOLD_ID</summary>
    [CustomPropertyDrawer(typeof(SHOP_ITEM_GOLD_ID))]
    public class Game_SHOP_ITEM_GOLD_ID_Drawer : BaseEditorID_Drawer<Game_SHOP_ITEM_GOLD_ID_Selector>
    {
        protected override Game_SHOP_ITEM_GOLD_ID_Selector GetSelector()
        {
            var w = ScriptableObject.CreateInstance<Game_SHOP_ITEM_GOLD_ID_Selector>();
            w.titleContent = new GUIContent("Select SHOP_ITEM_GOLD");
            w.ShowUtility();
            return w;
        }
    }
}

#endif