// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using Newtonsoft.Json;
using Devian;

namespace Devian.Domain.Operation
{
    // ================================================================
    // Table Entities
    // ================================================================

    /// <summary>PUSH_REMOTE row</summary>
    public sealed class PUSH_REMOTE : IEntityKey<string>
    {
        public string push_id { get; set; } = string.Empty;
        public string topic { get; set; } = string.Empty;
        public string language { get; set; } = string.Empty;
        public string default_msg { get; set; } = string.Empty;
        public bool is_test { get; set; }

        public string GetKey() => push_id;
    }

    /// <summary>PUSH_LOCAL row</summary>
    public sealed class PUSH_LOCAL : IEntityKey<string>
    {
        public string push_id { get; set; } = string.Empty;
        public string title_text_id { get; set; } = string.Empty;
        public string body_text_id { get; set; } = string.Empty;
        public bool is_test { get; set; }

        public string GetKey() => push_id;
    }

    // ================================================================
    // Table Containers
    // ================================================================

    /// <summary>TB_PUSH_REMOTE container</summary>
    public static partial class TB_PUSH_REMOTE
    {
        private static readonly Dictionary<string, PUSH_REMOTE> _dict = new();
        private static readonly List<PUSH_REMOTE> _list = new();

        private static readonly Dictionary<string, List<PUSH_REMOTE>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, string> _groupPrimaryKey = new();
        private static readonly Dictionary<string, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<PUSH_REMOTE> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<PUSH_REMOTE> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<PUSH_REMOTE>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out string key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(string key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static PUSH_REMOTE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out PUSH_REMOTE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(PUSH_REMOTE row)
        {
            _list.Add(row);
            _dict[row.push_id] = row;
            var groupKey = row.language;
            _keyToGroup[row.push_id] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<PUSH_REMOTE>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<string>.Default.Compare(row.push_id, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.push_id;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.push_id;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<PUSH_REMOTE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<PUSH_REMOTE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<PUSH_REMOTE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_PUSH_LOCAL container</summary>
    public static partial class TB_PUSH_LOCAL
    {
        private static readonly Dictionary<string, PUSH_LOCAL> _dict = new();
        private static readonly List<PUSH_LOCAL> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<PUSH_LOCAL> GetAll() => _list;

        public static PUSH_LOCAL? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out PUSH_LOCAL? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(PUSH_LOCAL row)
        {
            _list.Add(row);
            _dict[row.push_id] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<PUSH_LOCAL>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<PUSH_LOCAL>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<PUSH_LOCAL>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    // ================================================================
    // Table ID Types (for Inspector binding)
    // ================================================================

    /// <summary>Inspector-bindable ID for PUSH_REMOTE</summary>
    [Serializable]
    public sealed class PUSH_REMOTE_ID
    {
        public string Value;

        public static implicit operator string(PUSH_REMOTE_ID id) => id.Value;
        public static implicit operator PUSH_REMOTE_ID(string value) => new PUSH_REMOTE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for PUSH_LOCAL</summary>
    [Serializable]
    public sealed class PUSH_LOCAL_ID
    {
        public string Value;

        public static implicit operator string(PUSH_LOCAL_ID id) => id.Value;
        public static implicit operator PUSH_LOCAL_ID(string value) => new PUSH_LOCAL_ID { Value = value };
    }

    /// <summary>Table ID validation extensions</summary>
    public static class TableIdExtensions
    {
        public static bool IsValid(this PUSH_REMOTE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this PUSH_LOCAL_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
    }

}