// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using Newtonsoft.Json;
using Devian;

namespace Devian.Domain.Game
{
    // ================================================================
    // Contracts
    // ================================================================

    /// <summary>GAME_MESSAGE_TYPE enum</summary>
    public enum GAME_MESSAGE_TYPE
    {
        NONE = 0,
        LOGIN = 1,
        STAGE_CLEAR = 3,
        STAGE_SCORE = 4,
        MISSION_CLEAR = 5,
        TEST_001 = 8001,
        TEST_002 = 8002,
        TEST_003 = 8003,
        TEST_004 = 8004,
        TEST_005 = 8005,
        ACHIEVE_UNLOCKED = 9000,
        ACHIEVE_001 = 9001,
        ACHIEVE_002 = 9002,
        ACHIEVE_003 = 9003,
        ACHIEVE_004 = 9004,
        ACHIEVE_005 = 9005,
    }

    /// <summary>GAME_MESSAGE_SAVE_TYPE enum</summary>
    public enum GAME_MESSAGE_SAVE_TYPE
    {
        NONE = 0,
        SESSION_SUM = 1,
        SESSION_MAX = 2,
        TOTAL_SUM = 3,
        TOTAL_MAX = 4,
    }

    /// <summary>LEADERBOARD_MODE enum</summary>
    public enum LEADERBOARD_MODE
    {
        NONE = 0,
        NORMAL = 1,
        HARDCORE = 2,
    }

    /// <summary>MISSION_TYPE enum</summary>
    public enum MISSION_TYPE
    {
        NONE = 0,
        DAY = 1,
        PASS_FREE = 2,
        PASS_PAID = 3,
    }

    /// <summary>MISSION_MESSAGE enum</summary>
    public enum MISSION_MESSAGE
    {
        NONE = 0,
        RUNTIME_INIT = 1,
        RUNTIME_PROGRESS = 2,
        RUNTIME_CLAIMABLE = 3,
        RUNTIME_REWARDED = 4,
        DAY_RESET = 5,
        ACHIEVE_LEVEL_UP = 6,
    }

    /// <summary>ACHIEVE_MESSAGE enum</summary>
    public enum ACHIEVE_MESSAGE
    {
        NONE = 0,
        RUNTIME_INIT = 1,
        RUNTIME_PROGRESS = 2,
        RUNTIME_CLAIMABLE = 3,
        RUNTIME_LEVEL_UP = 4,
        RUNTIME_REWARDED = 5,
        ACHIEVEMENT_UNLOCKED = 6,
    }

    /// <summary>ACHIEVE_TYPE enum</summary>
    public enum ACHIEVE_TYPE
    {
        DEFAULT = 0,
    }

    /// <summary>CURRENCY_TYPE enum</summary>
    public enum CURRENCY_TYPE
    {
        GOLD = 0,
        JEWEL_FREE = 1,
        JEWEL_PAID = 2,
        STAMINA = 3,
        FRIENDSHIP = 4,
        GUILD_COIN = 5,
        ARENA_COIN = 6,
    }

    /// <summary>REWARD_TYPE enum</summary>
    public enum REWARD_TYPE
    {
        CARD = 0,
        CURRENCY = 1,
        EQUIP = 2,
        HERO = 3,
        RENTAL = 4,
        SEASON_PASS = 5,
    }

    /// <summary>ADVERTISE_FORMAT enum</summary>
    public enum ADVERTISE_FORMAT
    {
        BANNER = 0,
        INTERSTITIAL = 1,
        REWARDED = 2,
        APP_OPEN = 3,
    }

    /// <summary>ADVERTISE_PROVIDER enum</summary>
    public enum ADVERTISE_PROVIDER
    {
        GOOGLE_MOBILE_ADS = 0,
        MOCK = 1,
    }

    /// <summary>RENTAL_TYPE enum</summary>
    public enum RENTAL_TYPE
    {
        NO_ADS = 0,
    }

    /// <summary>SEASON_PASS_TYPE enum</summary>
    public enum SEASON_PASS_TYPE
    {
        S2026_01 = 0,
    }

    /// <summary>STAT_TYPE enum</summary>
    public enum STAT_TYPE
    {
        NONE = 0,
        CARD_AMOUNT = 1,
        CARD_LEVEL = 2,
        EQUIP_LEVEL = 11,
        UNIT_AMOUNT = 20,
        UNIT_LEVEL = 21,
        UNIT_HP_MAX = 100,
    }

    /// <summary>PRODUCT_KIND enum</summary>
    public enum PRODUCT_KIND
    {
        CONSUMABLE = 0,
        SUBSCRIPTION = 1,
        SEASON_PASS = 2,
        RENTAL = 3,
    }

    /// <summary>UserType enum</summary>
    public enum UserType
    {
        Guest = 0,
        Member = 1,
        Admin = 2,
    }

    /// <summary>UserProfile class</summary>
    public sealed class UserProfile : IEntity
    {
        public int Id { get; set; }
        public string Name { get; set; } = string.Empty;
        public UserType UserType { get; set; }
    }

    // ================================================================
    // Table Entities
    // ================================================================

    /// <summary>ADVERTISE row</summary>
    public sealed class ADVERTISE : IEntityKey<string>
    {
        public string AdvertiseId { get; set; } = string.Empty;
        public ADVERTISE_FORMAT Format { get; set; }
        public ADVERTISE_PROVIDER Provider { get; set; }
        public string RewardGroupId { get; set; } = string.Empty;
        public bool IsActive { get; set; }
        public bool AutoLoad { get; set; }
        public int CooldownSec { get; set; }
        public string AndroidAdUnitId { get; set; } = string.Empty;
        public string IosAdUnitId { get; set; } = string.Empty;

        public string GetKey() => AdvertiseId;
    }

    /// <summary>MESSAGE row</summary>
    public sealed class MESSAGE : IEntityKey<string>
    {
        public string MessageId { get; set; } = string.Empty;
        public GAME_MESSAGE_TYPE MessageType { get; set; }
        public GAME_MESSAGE_SAVE_TYPE SaveType { get; set; }

        public string GetKey() => MessageId;
    }

    /// <summary>MISSION row</summary>
    public sealed class MISSION : IEntityKey<string>
    {
        public string MissionId { get; set; } = string.Empty;
        public MISSION_TYPE MissionType { get; set; }
        public bool IsActive { get; set; }
        public bool Fixed { get; set; }
        public int OrderNum { get; set; }
        public string MessageId { get; set; } = string.Empty;
        public CBigInt? ConditionValue { get; set; }
        public string RewardGroupId { get; set; } = string.Empty;

        public string GetKey() => MissionId;
    }

    /// <summary>ACHIEVE row</summary>
    public sealed class ACHIEVE : IEntityKey<int>
    {
        public int Index { get; set; }
        public string AchieveId { get; set; } = string.Empty;
        public ACHIEVE_TYPE AchieveType { get; set; }
        public bool IsActive { get; set; }
        public int Level { get; set; }
        public int OrderNum { get; set; }
        public string MessageId { get; set; } = string.Empty;
        public CBigInt? ConditionValue { get; set; }
        public string RewardGroupId { get; set; } = string.Empty;
        public string AppleAchievementId { get; set; } = string.Empty;
        public string GoogleAchievementId { get; set; } = string.Empty;

        public int GetKey() => Index;
    }

    /// <summary>LEADERBOARD row</summary>
    public sealed class LEADERBOARD : IEntityKey<string>
    {
        public string LeaderboardId { get; set; } = string.Empty;
        public bool IsActive { get; set; }
        public string MessageId { get; set; } = string.Empty;
        public string AppleLeaderboardId { get; set; } = string.Empty;
        public string GoogleLeaderboardId { get; set; } = string.Empty;
        public string SeasonId { get; set; } = string.Empty;
        public LEADERBOARD_MODE Mode { get; set; }
        public long SeasonStartUtcMs { get; set; }
        public long SeasonEndUtcMs { get; set; }

        public string GetKey() => LeaderboardId;
    }

    /// <summary>LEADERBOARD_REWARD row</summary>
    public sealed class LEADERBOARD_REWARD : IEntityKey<int>
    {
        public int Index { get; set; }
        public string LeaderboardId { get; set; } = string.Empty;
        public long RankFrom { get; set; }
        public long RankTo { get; set; }
        public string RewardGroupId { get; set; } = string.Empty;

        public int GetKey() => Index;
    }

    /// <summary>EQUIP row</summary>
    public sealed class EQUIP : IEntityKey<string>
    {
        public string EquipId { get; set; } = string.Empty;
        public string NameId { get; set; } = string.Empty;
        public string DescId { get; set; } = string.Empty;

        public string GetKey() => EquipId;
    }

    /// <summary>CARD row</summary>
    public sealed class CARD : IEntityKey<string>
    {
        public string CardId { get; set; } = string.Empty;
        public string NameId { get; set; } = string.Empty;
        public string DescId { get; set; } = string.Empty;

        public string GetKey() => CardId;
    }

    /// <summary>PRODUCT row</summary>
    public sealed class PRODUCT : IEntityKey<string>
    {
        public string InternalProductId { get; set; } = string.Empty;
        public string RewardGroupId { get; set; } = string.Empty;
        public PRODUCT_KIND Kind { get; set; }
        public string Title { get; set; } = string.Empty;
        public bool IsActive { get; set; }
        public string StoreSkuApple { get; set; } = string.Empty;
        public string StoreSkuGoogle { get; set; } = string.Empty;

        public string GetKey() => InternalProductId;
    }

    /// <summary>REWARD row</summary>
    public sealed class REWARD : IEntityKey<int>
    {
        public int RewardNum { get; set; }
        public string RewardGroupId { get; set; } = string.Empty;
        public REWARD_TYPE Type { get; set; }
        public string Id { get; set; } = string.Empty;
        public int Amount { get; set; }

        public int GetKey() => RewardNum;
    }

    /// <summary>UNIT_HERO row</summary>
    public sealed class UNIT_HERO : IEntityKey<string>
    {
        public string UnitId { get; set; } = string.Empty;
        public string NameId { get; set; } = string.Empty;
        public string DescId { get; set; } = string.Empty;
        public int MaxHp { get; set; }

        public string GetKey() => UnitId;
    }

    /// <summary>UNIT_MONSTER row</summary>
    public sealed class UNIT_MONSTER : IEntityKey<string>
    {
        public string UnitId { get; set; } = string.Empty;
        public string NameId { get; set; } = string.Empty;
        public string DescId { get; set; } = string.Empty;
        public int MaxHp { get; set; }

        public string GetKey() => UnitId;
    }

    // ================================================================
    // Table Containers
    // ================================================================

    /// <summary>TB_ADVERTISE container</summary>
    public static partial class TB_ADVERTISE
    {
        private static readonly Dictionary<string, ADVERTISE> _dict = new();
        private static readonly List<ADVERTISE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<ADVERTISE> GetAll() => _list;

        public static ADVERTISE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out ADVERTISE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ADVERTISE row)
        {
            _list.Add(row);
            _dict[row.AdvertiseId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ADVERTISE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ADVERTISE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ADVERTISE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_MESSAGE container</summary>
    public static partial class TB_MESSAGE
    {
        private static readonly Dictionary<string, MESSAGE> _dict = new();
        private static readonly List<MESSAGE> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<MESSAGE> GetAll() => _list;

        public static MESSAGE? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out MESSAGE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(MESSAGE row)
        {
            _list.Add(row);
            _dict[row.MessageId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<MESSAGE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<MESSAGE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<MESSAGE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_MISSION container</summary>
    public static partial class TB_MISSION
    {
        private static readonly Dictionary<string, MISSION> _dict = new();
        private static readonly List<MISSION> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<MISSION> GetAll() => _list;

        public static MISSION? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out MISSION? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(MISSION row)
        {
            _list.Add(row);
            _dict[row.MissionId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<MISSION>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<MISSION>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<MISSION>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_ACHIEVE container</summary>
    public static partial class TB_ACHIEVE
    {
        private static readonly Dictionary<int, ACHIEVE> _dict = new();
        private static readonly List<ACHIEVE> _list = new();

        private static readonly Dictionary<string, List<ACHIEVE>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<ACHIEVE> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<ACHIEVE> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<ACHIEVE>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static ACHIEVE? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out ACHIEVE? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(ACHIEVE row)
        {
            _list.Add(row);
            _dict[row.Index] = row;
            var groupKey = row.AchieveId;
            _keyToGroup[row.Index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<ACHIEVE>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.Index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.Index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.Index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<ACHIEVE>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<ACHIEVE>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<ACHIEVE>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_LEADERBOARD container</summary>
    public static partial class TB_LEADERBOARD
    {
        private static readonly Dictionary<string, LEADERBOARD> _dict = new();
        private static readonly List<LEADERBOARD> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<LEADERBOARD> GetAll() => _list;

        public static LEADERBOARD? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out LEADERBOARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(LEADERBOARD row)
        {
            _list.Add(row);
            _dict[row.LeaderboardId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<LEADERBOARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<LEADERBOARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<LEADERBOARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_LEADERBOARD_REWARD container</summary>
    public static partial class TB_LEADERBOARD_REWARD
    {
        private static readonly Dictionary<int, LEADERBOARD_REWARD> _dict = new();
        private static readonly List<LEADERBOARD_REWARD> _list = new();

        private static readonly Dictionary<string, List<LEADERBOARD_REWARD>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<LEADERBOARD_REWARD> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<LEADERBOARD_REWARD> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<LEADERBOARD_REWARD>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static LEADERBOARD_REWARD? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out LEADERBOARD_REWARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(LEADERBOARD_REWARD row)
        {
            _list.Add(row);
            _dict[row.Index] = row;
            var groupKey = row.LeaderboardId;
            _keyToGroup[row.Index] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<LEADERBOARD_REWARD>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.Index, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.Index;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.Index;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<LEADERBOARD_REWARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<LEADERBOARD_REWARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<LEADERBOARD_REWARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_EQUIP container</summary>
    public static partial class TB_EQUIP
    {
        private static readonly Dictionary<string, EQUIP> _dict = new();
        private static readonly List<EQUIP> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<EQUIP> GetAll() => _list;

        public static EQUIP? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out EQUIP? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(EQUIP row)
        {
            _list.Add(row);
            _dict[row.EquipId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<EQUIP>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<EQUIP>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<EQUIP>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_CARD container</summary>
    public static partial class TB_CARD
    {
        private static readonly Dictionary<string, CARD> _dict = new();
        private static readonly List<CARD> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<CARD> GetAll() => _list;

        public static CARD? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out CARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(CARD row)
        {
            _list.Add(row);
            _dict[row.CardId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<CARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<CARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<CARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_PRODUCT container</summary>
    public static partial class TB_PRODUCT
    {
        private static readonly Dictionary<string, PRODUCT> _dict = new();
        private static readonly List<PRODUCT> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<PRODUCT> GetAll() => _list;

        public static PRODUCT? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out PRODUCT? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(PRODUCT row)
        {
            _list.Add(row);
            _dict[row.InternalProductId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<PRODUCT>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<PRODUCT>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<PRODUCT>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_REWARD container</summary>
    public static partial class TB_REWARD
    {
        private static readonly Dictionary<int, REWARD> _dict = new();
        private static readonly List<REWARD> _list = new();

        private static readonly Dictionary<string, List<REWARD>> _groupDict = new();
        private static readonly List<string> _groupList = new();
        private static readonly Dictionary<string, int> _groupPrimaryKey = new();
        private static readonly Dictionary<int, string> _keyToGroup = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
            _groupDict.Clear();
            _groupList.Clear();
            _groupPrimaryKey.Clear();
            _keyToGroup.Clear();
        }

        public static IReadOnlyList<REWARD> GetAll() => _list;

        public static IReadOnlyList<string> GetGroupKeys() => _groupList;

        public static IReadOnlyList<REWARD> GetByGroup(string groupKey)
        {
            return _groupDict.TryGetValue(groupKey, out var list) ? list : Array.Empty<REWARD>();
        }

        public static bool TryGetGroupPrimaryKey(string groupKey, out int key)
        {
            return _groupPrimaryKey.TryGetValue(groupKey, out key);
        }

        public static bool TryGetGroupKeyByKey(int key, out string groupKey)
        {
            return _keyToGroup.TryGetValue(key, out groupKey);
        }

        public static REWARD? Get(int key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(int key, out REWARD? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(REWARD row)
        {
            _list.Add(row);
            _dict[row.RewardNum] = row;
            var groupKey = row.RewardGroupId;
            _keyToGroup[row.RewardNum] = groupKey;
            if (!_groupDict.TryGetValue(groupKey, out var groupList))
            {
                groupList = new List<REWARD>();
                _groupDict[groupKey] = groupList;
                _groupList.Add(groupKey);
            }
            groupList.Add(row);
            if (_groupPrimaryKey.TryGetValue(groupKey, out var existing))
            {
                if (Comparer<int>.Default.Compare(row.RewardNum, existing) < 0)
                    _groupPrimaryKey[groupKey] = row.RewardNum;
            }
            else
            {
                _groupPrimaryKey[groupKey] = row.RewardNum;
            }
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<REWARD>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<REWARD>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<REWARD>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_HERO container</summary>
    public static partial class TB_UNIT_HERO
    {
        private static readonly Dictionary<string, UNIT_HERO> _dict = new();
        private static readonly List<UNIT_HERO> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<UNIT_HERO> GetAll() => _list;

        public static UNIT_HERO? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out UNIT_HERO? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_HERO row)
        {
            _list.Add(row);
            _dict[row.UnitId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_HERO>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_HERO>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_HERO>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    /// <summary>TB_UNIT_MONSTER container</summary>
    public static partial class TB_UNIT_MONSTER
    {
        private static readonly Dictionary<string, UNIT_MONSTER> _dict = new();
        private static readonly List<UNIT_MONSTER> _list = new();

        public static int Count => _list.Count;

        public static void Clear()
        {
            _dict.Clear();
            _list.Clear();
        }

        public static IReadOnlyList<UNIT_MONSTER> GetAll() => _list;

        public static UNIT_MONSTER? Get(string key)
        {
            return _dict.TryGetValue(key, out var row) ? row : null;
        }

        public static bool TryGet(string key, out UNIT_MONSTER? row)
        {
            return _dict.TryGetValue(key, out row);
        }

        private static void AddRow(UNIT_MONSTER row)
        {
            _list.Add(row);
            _dict[row.UnitId] = row;
        }

        public static void LoadFromJson(string json)
        {
            Clear();
            var rows = JsonConvert.DeserializeObject<List<UNIT_MONSTER>>(json);
            if (rows == null) return;
            foreach (var row in rows)
            {
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromNdjson(string ndjson)
        {
            Clear();
            using var reader = new StringReader(ndjson);
            string? line;
            while ((line = reader.ReadLine()) != null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER>(line);
                if (row == null) continue;
                AddRow(row);
            }
        }

        public static void LoadFromPb64Binary(byte[] rawBinary)
        {
            Clear();
            Pb64Loader.ParseRows(rawBinary, jsonRow =>
            {
                if (string.IsNullOrWhiteSpace(jsonRow)) return;
                var row = JsonConvert.DeserializeObject<UNIT_MONSTER>(jsonRow);
                if (row == null) return;
                AddRow(row);
            });
        }

        // ====================================================================
        // AfterLoad Hook (optional)
        // Called by DomainTableRegistry after TableManager inserts data.
        // ====================================================================

        internal static void _AfterLoad()
        {
            _OnAfterLoad();
        }

        static partial void _OnAfterLoad();
    }

    // ================================================================
    // Table ID Types (for Inspector binding)
    // ================================================================

    /// <summary>Inspector-bindable ID for ADVERTISE</summary>
    [Serializable]
    public sealed class ADVERTISE_ID
    {
        public string Value;

        public static implicit operator string(ADVERTISE_ID id) => id.Value;
        public static implicit operator ADVERTISE_ID(string value) => new ADVERTISE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for MESSAGE</summary>
    [Serializable]
    public sealed class MESSAGE_ID
    {
        public string Value;

        public static implicit operator string(MESSAGE_ID id) => id.Value;
        public static implicit operator MESSAGE_ID(string value) => new MESSAGE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for MISSION</summary>
    [Serializable]
    public sealed class MISSION_ID
    {
        public string Value;

        public static implicit operator string(MISSION_ID id) => id.Value;
        public static implicit operator MISSION_ID(string value) => new MISSION_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for ACHIEVE</summary>
    [Serializable]
    public sealed class ACHIEVE_ID
    {
        public int Value;

        public static implicit operator int(ACHIEVE_ID id) => id.Value;
        public static implicit operator ACHIEVE_ID(int value) => new ACHIEVE_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for LEADERBOARD</summary>
    [Serializable]
    public sealed class LEADERBOARD_ID
    {
        public string Value;

        public static implicit operator string(LEADERBOARD_ID id) => id.Value;
        public static implicit operator LEADERBOARD_ID(string value) => new LEADERBOARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for LEADERBOARD_REWARD</summary>
    [Serializable]
    public sealed class LEADERBOARD_REWARD_ID
    {
        public int Value;

        public static implicit operator int(LEADERBOARD_REWARD_ID id) => id.Value;
        public static implicit operator LEADERBOARD_REWARD_ID(int value) => new LEADERBOARD_REWARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for EQUIP</summary>
    [Serializable]
    public sealed class EQUIP_ID
    {
        public string Value;

        public static implicit operator string(EQUIP_ID id) => id.Value;
        public static implicit operator EQUIP_ID(string value) => new EQUIP_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for CARD</summary>
    [Serializable]
    public sealed class CARD_ID
    {
        public string Value;

        public static implicit operator string(CARD_ID id) => id.Value;
        public static implicit operator CARD_ID(string value) => new CARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for PRODUCT</summary>
    [Serializable]
    public sealed class PRODUCT_ID
    {
        public string Value;

        public static implicit operator string(PRODUCT_ID id) => id.Value;
        public static implicit operator PRODUCT_ID(string value) => new PRODUCT_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for REWARD</summary>
    [Serializable]
    public sealed class REWARD_ID
    {
        public int Value;

        public static implicit operator int(REWARD_ID id) => id.Value;
        public static implicit operator REWARD_ID(int value) => new REWARD_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_HERO</summary>
    [Serializable]
    public sealed class UNIT_HERO_ID
    {
        public string Value;

        public static implicit operator string(UNIT_HERO_ID id) => id.Value;
        public static implicit operator UNIT_HERO_ID(string value) => new UNIT_HERO_ID { Value = value };
    }

    /// <summary>Inspector-bindable ID for UNIT_MONSTER</summary>
    [Serializable]
    public sealed class UNIT_MONSTER_ID
    {
        public string Value;

        public static implicit operator string(UNIT_MONSTER_ID id) => id.Value;
        public static implicit operator UNIT_MONSTER_ID(string value) => new UNIT_MONSTER_ID { Value = value };
    }

    /// <summary>Table ID validation extensions</summary>
    public static class TableIdExtensions
    {
        public static bool IsValid(this ADVERTISE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this MESSAGE_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this MISSION_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this ACHIEVE_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this LEADERBOARD_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this LEADERBOARD_REWARD_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this EQUIP_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this CARD_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this PRODUCT_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this REWARD_ID? obj) => obj != null && !EqualityComparer<int>.Default.Equals(obj.Value, default);
        public static bool IsValid(this UNIT_HERO_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
        public static bool IsValid(this UNIT_MONSTER_ID? obj) => obj != null && !string.IsNullOrEmpty(obj.Value);
    }

}