// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#if UNITY_EDITOR

using UnityEditor;
using UnityEngine;
using Devian.Domain.Game;

namespace Devian
{
    /// <summary>Selector for GAME_MESSAGE_ID</summary>
    public class Game_GAME_MESSAGE_ID_Selector : BaseEditorID_Selector
    {
        protected override string GetDisplayTypeName() => "GAME_MESSAGE";

        public override void Reload()
        {
            ClearItems();
            TB_GAME_MESSAGE.Clear();

            var textAssets = AssetManager.FindAssets<TextAsset>("GAME_MESSAGE");
            foreach (var ta in textAssets)
            {
                var assetPath = AssetDatabase.GetAssetPath(ta);
                if (!assetPath.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase))
                    continue;

                TB_GAME_MESSAGE.LoadFromNdjson(ta.text);
                break;
            }

            foreach (var row in TB_GAME_MESSAGE.GetAll())
            {
                AddItem(row.GetKey().ToString());
            }
        }
    }

    /// <summary>PropertyDrawer for GAME_MESSAGE_ID</summary>
    [CustomPropertyDrawer(typeof(GAME_MESSAGE_ID))]
    public class Game_GAME_MESSAGE_ID_Drawer : BaseEditorID_Drawer<Game_GAME_MESSAGE_ID_Selector>
    {
        protected override Game_GAME_MESSAGE_ID_Selector GetSelector()
        {
            var w = ScriptableObject.CreateInstance<Game_GAME_MESSAGE_ID_Selector>();
            w.titleContent = new GUIContent("Select GAME_MESSAGE");
            w.ShowUtility();
            return w;
        }
    }
}

#endif