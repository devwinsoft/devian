// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

#if UNITY_EDITOR

using UnityEditor;
using UnityEngine;
using Devian.Domain.Game;

namespace Devian
{
    /// <summary>Selector for MISSION_ACHIEVE_ID</summary>
    public class Game_MISSION_ACHIEVE_ID_Selector : BaseEditorID_Selector
    {
        protected override string GetDisplayTypeName() => "MISSION_ACHIEVE";

        public override void Reload()
        {
            ClearItems();
            TB_MISSION_ACHIEVE.Clear();

            var textAssets = AssetManager.FindAssets<TextAsset>("MISSION_ACHIEVE");
            foreach (var ta in textAssets)
            {
                var assetPath = AssetDatabase.GetAssetPath(ta);
                if (!assetPath.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase))
                    continue;

                TB_MISSION_ACHIEVE.LoadFromNdjson(ta.text);
                break;
            }

            foreach (var groupKey in TB_MISSION_ACHIEVE.GetGroupKeys())
            {
                if (TB_MISSION_ACHIEVE.TryGetGroupPrimaryKey(groupKey, out var pk))
                {
                    // key = PK string (applied to Value), display = groupKey
                    AddItem(pk.ToString(), groupKey.ToString());
                }
            }
        }
    }

    /// <summary>PropertyDrawer for MISSION_ACHIEVE_ID</summary>
    [CustomPropertyDrawer(typeof(MISSION_ACHIEVE_ID))]
    public class Game_MISSION_ACHIEVE_ID_Drawer : BaseEditorID_Drawer<Game_MISSION_ACHIEVE_ID_Selector>
    {
        protected override Game_MISSION_ACHIEVE_ID_Selector GetSelector()
        {
            var w = ScriptableObject.CreateInstance<Game_MISSION_ACHIEVE_ID_Selector>();
            w.titleContent = new GUIContent("Select MISSION_ACHIEVE");
            w.ShowUtility();
            return w;
        }

        protected override string GetValueDisplayString(SerializedProperty valueProp)
        {
            // Show groupKey by default if available
            if (valueProp.propertyType == SerializedPropertyType.Integer)
            {
                var pk = valueProp.intValue;
                TB_MISSION_ACHIEVE.Clear();
                var textAssets = AssetManager.FindAssets<TextAsset>("MISSION_ACHIEVE");
                foreach (var ta in textAssets)
                {
                    var assetPath = AssetDatabase.GetAssetPath(ta);
                    if (!assetPath.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase))
                        continue;
                    TB_MISSION_ACHIEVE.LoadFromNdjson(ta.text);
                    break;
                }
                if (TB_MISSION_ACHIEVE.TryGetGroupKeyByKey(pk, out var groupKey))
                    return groupKey.ToString();
                return pk.ToString();
            }
            return base.GetValueDisplayString(valueProp);
        }
    }
}

#endif