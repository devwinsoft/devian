// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Text;
using Devian.Core;
using Google.Protobuf;

namespace Devian.Common
{
    /// <summary>
    /// VECTOR3 테이블 컨테이너.
    /// </summary>
    public sealed class TB_VECTOR3 : ITableContainer
    {
        private readonly Dictionary<float, VECTOR3Row> _map = new();
        private readonly List<VECTOR3Row> _rows = new();

        public IReadOnlyList<VECTOR3Row> Rows => _rows;
        public int Count => _rows.Count;

        public VECTOR3Row? Get(float key) => _map.TryGetValue(key, out var row) ? row : null;
        public bool TryGet(float key, out VECTOR3Row? row) => _map.TryGetValue(key, out row);
        public VECTOR3Row this[float key] => _map[key];
        public bool ContainsKey(float key) => _map.ContainsKey(key);

        /// <inheritdoc/>
        public void Clear()
        {
            _map.Clear();
            _rows.Clear();
        }

        /// <inheritdoc/>
        public void LoadFromJson(string json, LoadMode mode = LoadMode.Merge)
        {
            if (mode == LoadMode.Replace) Clear();

            var parser = new JsonParser(JsonParser.Settings.Default);
            var lines = json.Split('\n', StringSplitOptions.RemoveEmptyEntries);
            foreach (var line in lines)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;

                var msg = parser.Parse<Proto.VECTOR3Row>(line);
                var row = new VECTOR3Row();
                row._LoadProto(msg);

                var key = row.X;
                if (_map.ContainsKey(key))
                {
                    var idx = _rows.FindIndex(r => r.GetKey()!.Equals(key));
                    if (idx >= 0) _rows[idx] = row;
                }
                else
                {
                    _rows.Add(row);
                }
                _map[key] = row;
            }
        }

        /// <inheritdoc/>
        public string SaveToJson()
        {
            var sb = new StringBuilder();
            var formatter = new JsonFormatter(JsonFormatter.Settings.Default);

            foreach (var row in _rows)
            {
                var proto = row._SaveProto();
                sb.AppendLine(formatter.Format(proto));
            }

            return sb.ToString();
        }

        /// <inheritdoc/>
        public void LoadFromBase64(string base64, LoadMode mode = LoadMode.Merge)
        {
            if (mode == LoadMode.Replace) Clear();

            var bytes = Convert.FromBase64String(base64);
            var rows = Bootstrap._Decode_VECTOR3(bytes);
            foreach (var row in rows)
            {
                var key = row.X;
                if (_map.ContainsKey(key))
                {
                    var idx = _rows.FindIndex(r => r.GetKey()!.Equals(key));
                    if (idx >= 0) _rows[idx] = row;
                }
                else
                {
                    _rows.Add(row);
                }
                _map[key] = row;
            }
        }

        /// <inheritdoc/>
        public string SaveToBase64()
        {
            var bytes = Bootstrap._Encode_VECTOR3(_rows);
            return Convert.ToBase64String(bytes);
        }
    }
}
