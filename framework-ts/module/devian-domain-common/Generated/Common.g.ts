// <auto-generated>
// DO NOT EDIT - Generated by Devian Build System v10
// </auto-generated>

import type { Variant } from '../features/variant';
import { IEntity, IEntityKey } from '@devian/core';

// ========== Enums (gen:) ==========

/** Auto-generated enum from TB_COMPLEX_POLICY.key */
export enum ComplexPolicyType {
    AttackPower = 0,
    CriRate = 1,
    NameTag = 2,
}

/** Auto-generated enum from TB_ERROR_COMMON.id */
export enum CommonErrorType {
    CLOUDSAVE_CHECKSUM = 0,
    CLOUDSAVE_DELETE = 1,
    CLOUDSAVE_KEYIV = 2,
    CLOUDSAVE_LOAD = 3,
    CLOUDSAVE_NOCLIENT = 4,
    CLOUDSAVE_PAYLOAD_INVALID = 5,
    CLOUDSAVE_SAVE = 6,
    CLOUDSAVE_SIGNIN = 7,
    CLOUDSAVE_SLOT_EMPTY = 8,
    CLOUDSAVE_SLOT_MISSING = 9,
    COMMON_AUTH = 10,
    COMMON_INVALID_ARGUMENT = 11,
    COMMON_NETWORK = 12,
    COMMON_RATE_LIMIT = 13,
    COMMON_SERVER = 14,
    COMMON_UNKNOWN = 15,
    FIREBASE_APPLE_BRIDGE = 16,
    FIREBASE_APPLE_SIGNIN = 17,
    FIREBASE_APPLE_TOKEN = 18,
    FIREBASE_DEPENDENCY = 19,
    FIREBASE_FACEBOOK_NOT_IMPLEMENTED = 20,
    FIREBASE_GOOGLE_REAUTH_REQUIRED = 21,
    FIREBASE_GOOGLE_SIGNIN = 22,
    FIREBASE_GOOGLE_TOKEN = 23,
    FIREBASE_NOT_INITIALIZED = 24,
    FIREBASE_NO_USER = 25,
    FIREBASE_REAUTH_UNKNOWN = 26,
    FIREBASE_SIGNIN = 27,
    FIREBASE_TOKEN = 28,
    IAP_NOT_INITIALIZED = 29,
    IAP_NOT_SUPPORTED = 30,
    INVENTORY_DELTAS_NULL = 31,
    INVENTORY_DELTA_AMOUNT_NEGATIVE = 32,
    INVENTORY_DELTA_ID_EMPTY = 33,
    INVENTORY_DELTA_TYPE_INVALID = 34,
    INVENTORY_REFUND_INSUFFICIENT = 35,
    LOCALSAVE_CANCELLED = 36,
    LOCALSAVE_CHECKSUM = 37,
    LOCALSAVE_FILENAME_EMPTY = 38,
    LOCALSAVE_FILENAME_INVALID = 39,
    LOCALSAVE_KEYIV = 40,
    LOCALSAVE_PATH_EMPTY = 41,
    LOCALSAVE_READ = 42,
    LOCALSAVE_SLOT_EMPTY = 43,
    LOCALSAVE_SLOT_MISSING = 44,
    LOCALSAVE_WRITE = 45,
    LOGIN_ANONYMOUS_EXCEPTION = 46,
    LOGIN_ANONYMOUS_FAILED = 47,
    LOGIN_APPLE_EXCEPTION = 48,
    LOGIN_APPLE_LINK_FAILED = 49,
    LOGIN_APPLE_MISSING_TOKEN = 50,
    LOGIN_APPLE_SIGNIN_FAILED = 51,
    LOGIN_CREDENTIAL_UNSUPPORTED = 52,
    LOGIN_FIREBASE_DEPENDENCY = 53,
    LOGIN_GOOGLE_EXCEPTION = 54,
    LOGIN_GOOGLE_LINK_FAILED = 55,
    LOGIN_GOOGLE_MISSING_AUTH_CODE = 56,
    LOGIN_GOOGLE_SIGNIN_FAILED = 57,
    LOGIN_GPGS_AUTH_FAILED = 58,
    LOGIN_GPGS_EXCEPTION = 59,
    LOGIN_GPGS_NOT_FOUND = 60,
    LOGIN_GPGS_NOT_INITIALIZED = 61,
    LOGIN_GPGS_NO_AUTHENTICATE = 62,
    LOGIN_GPGS_NO_AUTH_CODE = 63,
    LOGIN_GPGS_NO_SERVER_ACCESS = 64,
    LOGIN_SYNC_CANCELLED = 65,
    LOGIN_SYNC_LOAD_CLOUD_FAILED = 66,
    LOGIN_SYNC_LOAD_LOCAL_FAILED = 67,
    LOGIN_SYNC_RESOLVE_FAILED = 68,
    LOGIN_SYNC_SAVE_CLOUD_FAILED = 69,
    LOGIN_SYNC_SAVE_LOCAL_FAILED = 70,
    LOGIN_SYNC_SLOT_LIST_FAILED = 71,
    LOGIN_UNSUPPORTED = 72,
    PURCHASE_ADJUSTMENTS_CALL_FAILED = 73,
    PURCHASE_ENTITLEMENTS_CALL_FAILED = 74,
    PURCHASE_FUNCTION_RESPONSE_INVALID = 75,
    PURCHASE_INIT_FAILED = 76,
    PURCHASE_INIT_REQUIRED = 77,
    PURCHASE_INTERRUPTED_SNAPSHOT_KIND_INVALID = 78,
    PURCHASE_INTERRUPTED_SNAPSHOT_PRODUCT_ID_MISSING = 79,
    PURCHASE_INTERRUPTED_STORAGE_UNAVAILABLE = 80,
    PURCHASE_NETWORK_UNAVAILABLE = 81,
    PURCHASE_PRODUCT_FETCH_FAILED = 82,
    PURCHASE_PRODUCT_NOT_FOUND = 83,
    PURCHASE_PURCHASE_IN_PROGRESS = 84,
    PURCHASE_PURCHASE_REQUEST_FAILED = 85,
    PURCHASE_RECENT_CALL_FAILED = 86,
    PURCHASE_RECENT_NOT_FOUND = 87,
    PURCHASE_REFUND_APPLY_FAILED = 88,
    PURCHASE_RENTAL_LATEST_CALL_FAILED = 89,
    PURCHASE_RENTAL_LATEST_NOT_FOUND = 90,
    PURCHASE_RESTORE_FAILED = 91,
    PURCHASE_SEASON_PASS_ALREADY_OWNED = 92,
    PURCHASE_STORE_CONFIRM_ACK_CALL_FAILED = 93,
    PURCHASE_STORE_FAILED = 94,
    PURCHASE_STORE_NOT_SET = 95,
    PURCHASE_UNAUTHENTICATED = 96,
    PURCHASE_UNSUPPORTED_PLATFORM = 97,
    PURCHASE_VERIFY_CALL_FAILED = 98,
    PURCHASE_VERIFY_FAILED_PRECONDITION = 99,
    PURCHASE_VERIFY_INVALID_ARGUMENT = 100,
    PURCHASE_VERIFY_REJECTED_UNKNOWN = 101,
    SAVEDATA_PAYLOAD_PARSE_FAILED = 102,
}

/** Auto-generated enum from TB_ERROR_SERVER.id */
export enum ServerErrorType {
    BAD_REQUEST = 1,
    FORBIDDEN = 3,
    INTERNAL = 100,
    NOT_FOUND = 4,
    SERVICE_UNAVAILABLE = 101,
    UNAUTHORIZED = 2,
    UNKNOWN = 0,
}

// ================================================================
// Tables
// ================================================================

export interface COMPLEX_POLICY extends IEntityKey<ComplexPolicyType> {
    Key: ComplexPolicyType;
    FallbackValue: Variant;
    MinValue: Variant;
    MaxValue: Variant;
    getKey(): ComplexPolicyType;
}

export interface ERROR_COMMON extends IEntityKey<CommonErrorType> {
    Id: CommonErrorType;
    Msg_key: string;
    Msg: string;
    getKey(): CommonErrorType;
}

export interface ERROR_SERVER extends IEntityKey<ServerErrorType> {
    Id: ServerErrorType;
    Code: number;
    Status: number;
    getKey(): ServerErrorType;
}

// ================================================================
// Table Containers
// ================================================================

export class TB_COMPLEX_POLICY {
    private static _dict: Map<ComplexPolicyType, COMPLEX_POLICY> = new Map();
    private static _list: COMPLEX_POLICY[] = [];

    static get count(): number { return this._list.length; }

    static clear(): void {
        this._dict.clear();
        this._list = [];
    }

    static getAll(): readonly COMPLEX_POLICY[] { return this._list; }

    static get(key: ComplexPolicyType): COMPLEX_POLICY | undefined {
        return this._dict.get(key);
    }

    static has(key: ComplexPolicyType): boolean {
        return this._dict.has(key);
    }

    static find(key: ComplexPolicyType): COMPLEX_POLICY {
        const row = this._dict.get(key);
        if (!row) throw new Error(`TB_COMPLEX_POLICY: key ${key} not found`);
        return row;
    }

    static tryFind(key: ComplexPolicyType): COMPLEX_POLICY | undefined {
        return this._dict.get(key);
    }

    static loadFromJson(json: string): void {
        this.clear();
        const lines = json.split('\n').filter(l => l.trim());
        for (const line of lines) {
            const row = JSON.parse(line) as COMPLEX_POLICY;
            this._list.push(row);
            this._dict.set(row.Key, row);
        }
    }

    static saveToJson(): string {
        return this._list.map(r => JSON.stringify(r)).join('\n');
    }
}

export class TB_ERROR_COMMON {
    private static _dict: Map<CommonErrorType, ERROR_COMMON> = new Map();
    private static _list: ERROR_COMMON[] = [];

    static get count(): number { return this._list.length; }

    static clear(): void {
        this._dict.clear();
        this._list = [];
    }

    static getAll(): readonly ERROR_COMMON[] { return this._list; }

    static get(key: CommonErrorType): ERROR_COMMON | undefined {
        return this._dict.get(key);
    }

    static has(key: CommonErrorType): boolean {
        return this._dict.has(key);
    }

    static find(key: CommonErrorType): ERROR_COMMON {
        const row = this._dict.get(key);
        if (!row) throw new Error(`TB_ERROR_COMMON: key ${key} not found`);
        return row;
    }

    static tryFind(key: CommonErrorType): ERROR_COMMON | undefined {
        return this._dict.get(key);
    }

    static loadFromJson(json: string): void {
        this.clear();
        const lines = json.split('\n').filter(l => l.trim());
        for (const line of lines) {
            const row = JSON.parse(line) as ERROR_COMMON;
            this._list.push(row);
            this._dict.set(row.Id, row);
        }
    }

    static saveToJson(): string {
        return this._list.map(r => JSON.stringify(r)).join('\n');
    }
}

export class TB_ERROR_SERVER {
    private static _dict: Map<ServerErrorType, ERROR_SERVER> = new Map();
    private static _list: ERROR_SERVER[] = [];

    static get count(): number { return this._list.length; }

    static clear(): void {
        this._dict.clear();
        this._list = [];
    }

    static getAll(): readonly ERROR_SERVER[] { return this._list; }

    static get(key: ServerErrorType): ERROR_SERVER | undefined {
        return this._dict.get(key);
    }

    static has(key: ServerErrorType): boolean {
        return this._dict.has(key);
    }

    static find(key: ServerErrorType): ERROR_SERVER {
        const row = this._dict.get(key);
        if (!row) throw new Error(`TB_ERROR_SERVER: key ${key} not found`);
        return row;
    }

    static tryFind(key: ServerErrorType): ERROR_SERVER | undefined {
        return this._dict.get(key);
    }

    static loadFromJson(json: string): void {
        this.clear();
        const lines = json.split('\n').filter(l => l.trim());
        for (const line of lines) {
            const row = JSON.parse(line) as ERROR_SERVER;
            this._list.push(row);
            this._dict.set(row.Id, row);
        }
    }

    static saveToJson(): string {
        return this._list.map(r => JSON.stringify(r)).join('\n');
    }
}